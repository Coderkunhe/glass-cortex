# GlassCortex 环境变量模板
# 复制到 .env 后填入实际密钥。.env 已在 .gitignore，不会提交。
#
# 用法：
#   Linux/Mac:      cp .env.example .env
#   Windows PS:     Copy-Item .env.example .env

# ─── LLM API Keys ───────────────────────────────────────────
# DeepSeek: 默认 LLM 提供商（src/config.py llm_base_url）
# 申请: https://platform.deepseek.com/
DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# QWEN (阿里 DashScope 兼容模式): 备用/多模型对比用
# 申请: https://dashscope.console.aliyun.com/
QWEN_API_KEY=sk-your-qwen-key-here

# StepFun: 备用/多模态用（audio/vision 模型）
# 申请: https://platform.stepfun.com/
STEPFUN_API_KEY=your-stepfun-key-here

# ─── HuggingFace 国内镜像 ──────────────────────────────────
# 国内服务器无法直连 huggingface.co 时，设置此变量切换到镜像站。
# 默认值: https://huggingface.co（src/config.py EmbedConfig.hf_endpoint）
# 国内部署推荐值: https://hf-mirror.com
# HF_ENDPOINT=https://hf-mirror.com

# ─── Admin 工程面板密码 ──────────────────────────────────
# 管理员面板登录密码（前端 PasswordGate 组件读取）。
# 默认值: Coder@9527（环境变量未设置时使用）。
# 生产部署建议覆盖为强密码。
# NEXT_PUBLIC_ADMIN_PASSWORD=your-admin-password-here

# ─── Frontend ────────────────────────────────────────────────
# API 基地址（前端 fetch 请求目标）。
# 默认值: http://localhost:8000（开发模式直连 FastAPI）
# 生产部署: 设为 /api（通过 nginx 反代）或完整 URL
# NEXT_PUBLIC_API_URL=http://localhost:8000

# ─── CORS ────────────────────────────────────────────────────
# 允许跨域访问的前端源列表，逗号分隔。
# 默认值: *（允许所有源，仅开发环境安全）
# 生产部署: 设为实际域名，如 https://your-domain.com
# CORS_ORIGINS=*

# ─── 离线部署（无外网环境）─────────────────────────────────
# 强制 HuggingFace 走本地缓存，禁止联网下载。
# 详见 deploy/offline-model.md
# TRANSFORMERS_OFFLINE=1
# HF_HUB_OFFLINE=1
