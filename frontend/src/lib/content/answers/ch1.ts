import type { Answer } from "../types";

/** 第 1 章：上下文工程 答案列表 */
export const CH1_ANSWERS: Answer[] = [
  {
    id: "q1.1",
    question: '用户输入内容超过上下文窗口限制，常见处理策略有哪些？每种策略的优缺点是什么？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P0",
    confidence: { l0: 0.97, l1: 0.95, l2: 0.92, l3: 0.9 },
    overallConfidence: 0.9,
    l0: '当上下文窗口塞不下时，有三种经典策略：截断最早的消息（FIFO）、按相关度保留最重要的、以及把旧内容压缩成摘要——没有银弹，选哪个取决于你更在乎「不丢重要信息」还是「不丢完整细节」。',
    l1: `想象你在一个只能放 10 张便签的桌面上工作。刚开始的时候，每张便签都能看清。写到第 11 张的时候——麻烦了，你得撕掉一张旧的才能腾出空间。撕哪张？

LLM 面对的[上下文窗口](https://baike.baidu.com/item/上下文窗口)（Context Window）就是这张桌子。默认大小通常是 4096 个 [token](https://baike.baidu.com/item/Token)——差不多 2000-3000 个中文字。当你的对话历史 + 系统提示 + 召回的记忆加起来超出这个限额，系统就必须做选择：扔掉一些，留下一些。这个选择过程叫「溢出处理」。

目前有三种主流策略，各有各的脾气：

### 策略一：FIFO 截断 (Truncate)

**最简单的做法：按时间顺序，最早进来的最先被扔掉。** 不排序、不比较、不纠结——就是一个先进先出的队列。

- **优点**：零额外计算开销，行为完全可预测。你永远知道哪些记忆还在窗口里（就是最近 N 条）。调试起来也最简单——出问题的时候你不用怀疑「是不是排序算法出了 bug」。
- **缺点**：它不管内容——一段三周前讨论你薪资要求的对话和一段三周前说「今天天气不错」的寒暄，在它眼里地位完全一样[^1]。重要的旧记忆可能被琐碎的新对话挤掉。而且窗口里的信息质量取决于对话的自然时序——如果最近的消息恰好都是废话，那就全是废话。

[^1]: 这在工程上恰恰是 FIFO 的优点——「不管内容」意味着零偏见、零额外延迟。只是在用户体验上它显得迟钝。

### 策略二：相关度优先 (Prioritize)

**不按时间，按「跟现在这个话题有多相关」排序。** 高相关度的记忆留在窗口里，低相关度的丢掉。

- **优点**：窗口里每条记忆都是精选过的，跟当前问题直接相关。相当于有人帮你把桌上无关的便签清走，只留跟当前任务相关的。
- **缺点**：排序本身需要计算——每条记忆跟当前查询做相似度比较，记忆多的时候这一步本身就在消耗时间和 token。而且「只保留最相关的」可能导致信息单一化——如果你在聊一个需要多角度思考的问题，窗口里清一色高度相关但观点雷同的记忆反而限制了 LLM 的发散能力。

### 策略三：压缩摘要 (Summarize)

**不上不下：高相关的保留原文，低相关的不是直接丢，而是压缩成一句话摘要塞回去。** 相当于你不能把整本书带进考场，但你带了一张手写的 cheat sheet。

- **优点**：信息损失最小——低相关的记忆虽然丢了细节，但「骨架」还在。如果 LLM 后来发现那条看似不相关的记忆其实关键，至少还有一句摘要可以追。
- **缺点**：这是三种策略中计算成本最高的——压缩需要额外调用 LLM（或者用模板摘要，但效果差很多）[^2]。压缩质量也参差不齐，特别是当原记忆本身很零碎的时候，摘要可能变成一句毫无信息量的废话。而且压缩后的摘要本身也占 token——极端情况下你费半天劲压缩省下的 token 还没压缩过程消耗的多。

[^2]: GlassCortex 项目中 \`src/chat/engine.py\` 的 \`compress_message()\` 方法就是一个 LLM 压缩实现，将旧消息压缩为一句话摘要。这是最贵但也最保真的方式。

\`\`\`mermaid
%% title: 图：三种上下文溢出处理策略
graph TD
    OVERFLOW{"上下文溢出<br/>total > window_size"}
    OVERFLOW -->|"策略一"| FIFO["FIFO 截断<br/>最早进入的先丢弃"]
    OVERFLOW -->|"策略二"| PRIO["相关度优先<br/>低分的先丢弃"]
    OVERFLOW -->|"策略三"| COMP["压缩摘要<br/>低分压缩为摘要"]
    FIFO --> R1["简单可靠 · 零计算开销<br/>不管内容重要性"]
    PRIO --> R2["窗口内容高质量<br/>但可能信息单一化"]
    COMP --> R3["信息损失最小<br/>但计算成本最高"]
\`\`\`

### 实际怎么选？

没有普适最优解。选策略取决于你对两个东西的权衡：**你更怕丢重要信息（选 Prioritize），还是更怕丢完整细节（选 Summarize）？如果你两者都不太怕——要的就是简单可靠——FIFO 反而是最好的答案。**

在 GlassCortex 的实现中，默认策略是 Prioritize（按相关度优先），但用户可以在侧边栏随时切换。这个设计决策的底层逻辑是：对于「AI 记忆助手」这个场景，用户更在乎 AI 记得跟当前话题相关的事，而不是公平地记住所有事。

> 置信度：0.95`,
    l2: `### 三种策略的代码实现对比

在 GlassCortex 中，溢出策略的纯计算引擎在 \`src/context/overflow_sim.py\` 中实现（~440 行）。它被刻意设计为**纯函数**——不调用 LLM、不写数据库、不产生副作用。这意味着你可以在 Lab 页的沙箱里反复跑而不花一分钱 API 费用。

核心函数 \`simulate_overflow()\` 接收三个参数：召回的记忆列表、上下文窗口大小、策略名称。返回 \`OverflowSimResult\`，包含保留的记忆列表、被丢弃/压缩的记忆列表、以及模拟后的 token 统计。

策略人格化的定义也在同一个文件中：

| 策略 | 人格名 | 规则 |
|------|--------|------|
| Truncate | 守门员 | 严格先到先出，最早进入的先丢弃 |
| Prioritize | 策展人 | 按相关度排序，只保留与当前话题最相关的 |
| Summarize | 口述史家 | 高相关保留 + 低相关压缩为一句话摘要 |

实际的 LLM 对话中，\`src/chat/engine.py\` 的 \`_build_system_prompt()\` 方法在组装系统提示词之前调用 \`simulate_overflow()\`。如果溢出被触发：
1. 按策略选出保留和丢弃/压缩的记忆
2. 保留的记忆注入系统提示词的「对话记忆」段
3. 丢弃的记忆从上下文窗口消失
4. 如果是 Summarize 策略，压缩后的摘要注入「历史摘要」段

上下文末端的四色分区条（\`src/web/components/_context.py\`）会可视化溢出结果——溢出部分用虚线标注「优化前记忆区 N tokens（压缩节省 M tokens）」，让你直观看到溢出帮你省下了多少空间。

### 三种策略的性能对比

| 维度 | FIFO 截断 | 相关度优先 | 压缩摘要 |
|------|----------|-----------|---------|
| 计算成本 | 零 | 低（向量相似度比较） | 高（额外 LLM 调用） |
| 信息保真度（重要信息） | 低（不问内容） | 高 | 最高（低相关也有骨架） |
| 信息保真度（完整细节） | 高（保留即完整） | 中（丢弃即丢失） | 中（摘要损失细节） |
| 可预测性 | 最高 | 中 | 最低 |
| Token 节省效果 | 固定（超多少丢多少） | 固定 | 浮动（取决于压缩比） |
| 适合场景 | 简单问答、短对话 | 知识查询、话题聚焦 | 长对话、多轮探索 |

> 置信度：0.92`,
    l3: `### 当前行业实践

三种基础策略在业界都可以找到对应实现，但越来越多系统在使用**混合策略**：

- **OpenAI 的 Chat API**：在底层使用的是滑动窗口 + 智能截断——不完全是纯 FIFO，因为它会对系统提示词做特殊保护（system prompt 常驻窗口，不参与截断）。
- **LangChain / LlamaIndex**：提供了多种「记忆」（Memory）类型——ConversationBufferMemory（FIFO）、ConversationSummaryMemory（压缩）、ConversationTokenBufferMemory（混合），本质上是三种策略的参数化封装。
- **MemGPT**（2023）：把操作系统虚拟内存管理的思路搬到了 LLM 上下文管理——「热层」在上下文窗口内、「温层」在会话内存中、「冷层」持久化到磁盘，三层之间有自动迁移逻辑。这是目前最接近「上下文 OS」的方案。

### 未解决的问题

1. **自动策略选择**：目前策略是人为选的。系统能不能自己判断「这场对话适合用 FIFO」「那场对话应该切换到 Summarize」？触发条件是什么——对话轮次？话题漂移速度？用户情绪？

2. **信息损失度量**：我们只能统计「丢了 N tokens」，但没法量化「丢掉的 N tokens 里有多少真正重要的信息」。没有好的信息损失度量，策略对比就只能靠直觉和用户反馈。

3. **压缩质量保证**：LLM 压缩的效果高度不稳定——同一条消息压两遍可能产生完全不同的摘要。如何保证压缩输出的确定性？是否需要在压缩后做「解压-比对」来检查信息保真度？

4. **跨会话上下文连续性**：现在的溢出策略只在单次会话内工作。如果用户在两次不同对话中讨论同一话题，第一次对话末尾的记忆可能因为溢出被丢弃，第二次对话就无法访问。是否需要在会话之间传递「上下文遗书」？

5. **动态窗口预算**：当前场景下 system/recall/history/tools 四区是按固定规则分配的。是否可以根据查询意图动态调整？——比如「指令类」查询不需要太多记忆召回，可以把 history 区调大。

### GlassCortex 的后续方向

项目已在溢出策略上完成两项升级：
- **Token 预算机制**：TokenLedger（\`src/token_ledger.py\`）已实现全链路 token 计量，每次 LLM 调用自动记录 prompt/completion tokens。四区动态配比（system/recall/history/tools）通过 \`compute_partitions()\` 实时计算，前端 ContextBar 可视化
- **按需展开**：Phase 58 已交付两阶段注入——\`ChatEngine.generate(two_stage=True)\` Stage 1 注入 80 字符概要 + [ref:N] 标签，Stage 2 通过 \`expand_references()\` 按需展开完整内容。API 层面的两阶段集成待后续完成

> 置信度：0.90`,
    labLinks: [{ tab: "context", label: "溢出模拟沙箱" }],
  },
  {
    id: "q1.2",
    question: '如果用户输入未超过上下文窗口，但要求输出内容带原文由此超出了上下文窗口限制，应该如何处理？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P0",
    confidence: { l0: 0.95, l1: 0.92, l2: 0.88, l3: 0.85 },
    overallConfidence: 0.85,
    l0: '输入刚好塞下，但输出要带上原文——就跟桌子能放下材料，但写报告时要把整份材料抄进去一样，纸不够了。应对方案分三种：拆成小块分批处理、把原文压缩成要点再输出、或告诉用户「写到这里纸不够了」。',
    l1: `想象你在那张只能放 10 张便签的桌面上工作。这次你没有超载——刚好 10 张，一张不多。问题是，你的老板让你写一份总结报告，而且要「每条便签的内容都写进去，附上原文引用」。

这就有意思了。**输入没超，但输出需要引用输入的全部内容，加起来就超了。**

在 LLM 的世界里，[上下文窗口](https://baike.baidu.com/item/上下文窗口)是一个总计容量——输入（系统提示 + 用户消息 + 召回记忆）和输出（模型生成的回复）共享同一块空间（如 DeepSeek 的 64k token）。大部分 API 通过 \`max_tokens\` 参数为输出预留一个固定预算（比如 4096 token），剩下的才是输入的可用容量。

当用户要求「把这篇 5000 字的文章翻译成英文，并附上原文对照」时，即使输入勉强塞得进，输出也会超出 \`max_tokens\` 限制。模型被迫中途截断——你看到一句没说完的话，后面写着「…」。

有三种应对思路，按破坏性递增排列：

### 方案一：分块处理 (Chunking)

**核心思想：把大任务切小，分批处理，最后拼接。**

就像你不可能一次抄完一本百科全书，但你可以一次抄一页，最后订成一本。

在工程上，这对应着：
1. 把长文本按语义边界（段落 / 章节 / 固定 token 数）切分为 N 块
2. 每块独立交给 LLM 处理（带上相同的系统指令和原文块）
3. 最后汇总各块结果为完整输出

- **优点**：理论上可以处理任意长度的文本，不受窗口限制。每一块的处理质量最高（不截断、不压缩）。
- **缺点**：调用成本线性增长——N 块需要 N 次 LLM 调用。汇总步骤需要额外一次调用或拼接逻辑。而且跨块上下文丢失——如果模型需要在第 5 块中引用第 2 块的信息，做不到。

实际落地中，GlassCortex 的 \`ChatEngine.generate()\` 已通过 \`two_stage\` 参数支持两阶段注入——Stage 1 将召回条目压缩为 80 字符概要 + [ref:N] 引用标签，Stage 2 通过 \`expand_references()\` 按需展开。此功能在引擎层已就绪（Phase 58），API 层面的集成待后续完成。

### 方案二：压缩输出 (Output Summarization)

**核心思想：原文保留在输入中，输出只包含处理后的结果，不完整引用原文。**

相当于你的老板说「总结每张便签的要点就行，不用逐字抄」。

在 LLM 场景中，这意味着：
1. 在指令中明确要求「不要逐字引用原文，总结要点」
2. 模型输出大幅减少——从「原文 + 翻译」变为「翻译（附重要术语原文对照）」
3. 输出量压缩到 \`max_tokens\` 可以容纳的范围

- **优点**：零额外 API 调用，单次完成。输出质量往往更高——压缩迫使模型筛选关键信息，反而避免了「信息过载」。
- **缺点**：不适用于需要完整原文输出的场景（如精确翻译、法律文书审核——你不能只给客户「这是合同的要点概括」）。

### 方案三：截断 + 用户告知 (Truncate & Notify)

**核心思想：输出能写多少写多少，然后告诉用户「我没写完」。**

这是最朴素的做法——模型写到 \`max_tokens\` 满了就停，响应末尾加「…」或系统自动追加「输出被截断，以下内容未完整生成」。

- **优点**：零工程成本，是 LLM API 的默认行为。
- **缺点**：用户体验最差——用户读一半发现没了。而且用户不知道是截断了还是模型答完了，容易产生困惑。

### 实际怎么选？

**分块处理最「正统」但最贵，压缩输出性价比最高，截断告知是兜底。** GlassCortex 目前的策略是默认不做分块——因为大部分用户输入原文不超过 2000 字，远低于窗口限制。输出压缩通过系统提示词的 \`concise\` 风格来实现。分块管线（用户输入超长时的自动切分处理）归入远期规划；召回记忆的按需展开（两阶段注入，Phase 58）已实现。

> 置信度：0.92

	\`\`\`mermaid
%% title: 图：输出超出窗口的三种应对策略对比
graph TD
    IN["📤 输入正常<br/>但输出需要引用原文"]
    IN --> OVERFLOW{"📊 输出超出 max_tokens？"}
    OVERFLOW -->|"方案一"| CHUNK["🧩 分块处理<br/>长文本切 N 块 → 分批 → 拼接<br/>成本最高 · 质量最优"]
    OVERFLOW -->|"方案二"| COMPRESS["📝 压缩输出<br/>原文保留，压缩要点输出<br/>性价比最高 · 零额外调用"]
    OVERFLOW -->|"方案三"| TRUNCATE["✂️ 截断告知<br/>写满即停 · 追加截断标记<br/>零成本 · 体验最差"]
    CHUNK --> R1["适合：法律审核、精确翻译<br/>跨块上下文会丢失"]
    COMPRESS --> R2["适合：日常对话、总结<br/>GlassCortex 默认策略"]
    TRUNCATE --> R3["兜底：写到一半停了<br/>用户困惑"]
    style IN fill:#4f46e5,stroke:#4338ca,color:#fff
    style CHUNK fill:#3b82f6,stroke:#2563eb,color:#fff
    style COMPRESS fill:#34d399,stroke:#059669,color:#111
    style TRUNCATE fill:#f59e0b,stroke:#d97706,color:#111
	\`\`\`

> 置信度：0.92`,
    l2: `### 输出溢出的工程视角

输出溢出的本质是 **token 预算的二象性**——输入和输出在同一物理窗口中竞争。

在 \`src/context/overflow_sim.py\` 的当前实现中，溢出检测 \`simulate_overflow()\` 仅检查 \`total_needed > window_size\`，其中 \`total_needed = system_tokens + recall_tokens + history_tokens + tools_tokens\`。它**没有考虑输出 token**。这意味着：如果输入刚好塞满窗口，模型输出必定触发截断——但溢出模拟器发现「没有溢出」，因为它在计算时排除了输出。

这是一个已知的工程缺口。修正方向是加入 \`output_budget\` 参数：

\`\`\`python
# 伪代码示意：输出感知的溢出检测
def simulate_overflow_with_output(
    partitions: ContextPartitions,
    window_size: int,
    output_budget: int,      # 新增：max_tokens 预留
) -> OverflowSimResult:
    available_input = window_size - output_budget
    total_needed = sum(p.tokens for p in partitions)
    if total_needed > available_input:
        # 触发策略处理
        ...
\`\`\`

但这个修改还没排入计划——因为它会改变现有溢出策略的行为基线。

### 分块处理的工程成本

将文本分成 N 块意味着：
- LLM 调用次数 ×N
- Token 消耗 ≈ 输入（每块重复系统指令）× N + 输出（各块结果）× N + 汇总（1 次）
- 以 64k 窗口、5000 字原文为例：分 3 块 ≈ 4 次调用（3 处理 + 1 汇总），总 token ≈ 原文 7k × 3 + 输出 6k × 3 + 汇总 2k ≈ 41k token，远低于单次处理但可能出现截断时的成本 ≈ 原文 7k + 输出截断 4k = 11k token 但质量不完整

### 流式输出的特殊考虑

流式 API（SSE / Server-Sent Events）下，输出截断不是硬停——模型在任何时候都可能因为输出超过 \`max_tokens\` 而主动结束流。这意味着：
- 你无法等输出完成再决定「要不要压缩」——必须在调用前就决策
- 流式响应末尾的截断检测可以通过检查 \`finish_reason: "length"\` 实现
- 检测到截断后可以自动追加「[输出被截断，剩余内容未生成]」标记

在 GlassCortex 的 \`api/routers/chat.py\` 中，流式响应已使用 \`StreamingResponse\` 逐块转发，但截断标记尚未实现——这属于远期上下文治理规划。

> 置信度：0.88`,
    l3: `### 输出溢出的深层问题

输出溢出表面上是一个 token 预算问题，但它揭示了 LLM 系统设计中一个更根本的张力：**模型生成的内容本身就是上下文的一部分，系统在设计上下文策略时却不考虑即将生成的内容。**

这导致了几个有趣的理论问题：

**问题 1：输出的「自反性」**

当模型回答一个需要引用原文的问题时，它的输出正在占用它自己最后的思考空间。DeepSeek R1 的论文中提到了一个现象：在长输出场景中，模型末尾部分的质量显著低于开头——不是因为模型在「疲劳」，而是因为剩余 token 不多了，模型在紧急收尾。

这种「自反性」意味着：系统的输出策略决策（max_tokens 设置、是否需要引用原文）应该在输入阶段就做出，而不是等到输出阶段被动响应。

**问题 2：分块处理的上下文独立性问题**

分块处理在工程上可行，但有一个语义上的致命伤——每个块的处理是独立的，模型无法在看到块 5 的时候利用块 2 中发现的线索。这不只是「跨块引用」问题，而是更深层的：

- **模式发现受阻**：模型需要全局视角才能发现的模式（如「这篇文章的前后观点自相矛盾」）在分块模式下永远不可能出现
- **一致性保证缺失**：各块输出可能在风格、结论上互相矛盾
- **排序敏感性**：如果块的处理顺序影响结果（先看「结论」和先看「证据」导致的判断不同），分块策略的选择本身就成了一个偏见源

学术界对此的研究方向包括：
- **分层摘要与迭代细化**：先对各块做粗粒度摘要，再进行跨块分析——Google 的 PEGASUS 模型展示了这种「摘要→再摘要」范式的可行性
- **滑动窗口 + 注意力复用**：各块共享 Key-Value Cache，避免重复计算早期层的表示——Longformer 和 BigBird 的稀疏注意力是这一路线的先驱

**问题 3：输出溢出的「后悔成本」**

这是 q4.10 已经讨论过的概念在输出端的映射。当输出被截断时：
- 已消耗的输入 token 全部沉没（无法重放输入而不用重新支出）
- 已消耗的输出 token 部分沉没（截断前的输出是有价值的，但不完整）
- 用户感知体验下降，可能需要第二次输入「刚才没说完，继续」
- 而第二次输入的 token 成本 + 重新生成输出的成本 > 第一次就分块处理

所以输出溢出的最佳策略不是「发生时怎么处理」，而是**预测它会否发生并在发生前切换策略**。这需要：

1. 一个输出长度预测器（基于 prompt 复杂度、输出格式要求、历史类似请求的输出长度）
2. 预测到溢出风险时，自动降级为分块模式或压缩输出模式
3. 对用户透明化降级决策（「因为输出可能超长，已自动切换为分块处理模式」）

GlassCortex 目前没有实现这个预测器——TokenLedger 可以告诉你「上次类似的请求花了多少 token」，但无法在本次调用前精确预测。这是远期四支柱研究中的一个开放问题。

> 置信度：0.85`,
    labLinks: [{ tab: "context", label: "溢出模拟沙箱" }],
  },
  {
    id: "q1.3",
    question: '如何处理上下文窗口中的重复信息？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P0",
    confidence: { l0: 0.96, l1: 0.93, l2: 0.90, l3: 0.87 },
    overallConfidence: 0.87,
    l0: '重复信息像回音一样占用窗口空间：同一个记忆被反复召回、用户多次表达同一意思、或多轮对话中重复的指令。处理手段有三层：在召回时用 MMR 去重、在组装时检测重复并合并、在对话过程中压缩冗余历史。',
    l1: `上下文窗口是有限资源，重复信息就是最隐蔽的浪费方式。不像截断或溢出那样一眼能看出来，重复悄无声息地吃掉 token，等你发现的时候窗口已经占满——里面装的全是同一个意思的三四种说法。

### 重复信息从哪来？

**来源一：召回阶段的多路返回**

这是最常见的来源。记忆系统在召回时可能从多个索引中返回同一事实的不同版本——FAISS 向量索引返回了「用户喜欢 Python」，SQLite 全文搜索也返回了「用户喜欢 Python」，甚至来自不同对话轮次的重复调用。在 \`src/memory/recall.py\` 中，MMR（最大边际相关性）机制就是专门处理这个的——它确保返回的记忆在语义上彼此不同，而不是一堆雷同的结果。

**来源二：用户的多轮重述**

用户可能在对话的不同轮次用不同方式表达同一个意思：
- 第 3 轮：「我是做前端开发的」
- 第 8 轮：「我主要写 React」
- 第 12 轮：「我是前端工程师」

这三条都指向同一个事实「用户是前端工程师」，但窗口里占了三条的空间。系统没有机制识别它们是同一件事——因为 \`src/memory/triple.py\` 的事实抽取是在单轮内做的，不跨轮关联。

**来源三：系统指令的重复注入**

某些组件在每次对话轮次都会注入相同的指令或记忆摘要——"你是一个 AI 助手"这样的系统提示每轮都有，但框架代码可能在不同阶段重复注入类似的内容。GlassCortex 的 \`_build_system_prompt()\` 通过缓存系统提示词来缓解这个问题，但不同组件（Planner、记忆引擎、上下文工程）各自注入的指令中可能包含重叠内容。

### 三层处理方法

**第一层：召回阶段去重（提前预防）**

最理想的处理阶段。在记忆从存储中取出、还没进入上下文窗口时，就用去重机制过滤。

上文提到的 MMR 算法天然具有去重能力：它计算每条记忆与已选集合的最大相似度，只选与已有记忆足够不同的。在 GlassCortex 中，\`mmr_rerank()\` 函数的 \`lambda\` 参数控制多样性的权重——\`lambda=0.7\` 意味着 70% 看相关度、30% 看多样性。调高 lambda 的去重效果更强，但可能漏掉真正相关的记忆。

**第二层：组装阶段检测（执行中纠偏）**

在构造上下文时，逐条检查即将注入的内容是否与已选内容重叠。

这需要：
1. 对每条新内容做语义哈希或向量相似度比较
2. 发现高度重叠时，合并或丢弃重复项
3. 在 \`OverflowSimResult\` 的 \`dropped_items\` 中记录丢弃原因

GlassCortex 当前没有实现显式的组装阶段去重——\`simulate_overflow()\` 只是按策略筛选，不检查内容是否重复。这是一个可改进点。

**第三层：对话历史压缩（事后清理）**

对已有对话历史做压缩，把多轮相似的表达合并成一个简洁的表示。

\`src/chat/engine.py\` 的 \`compress_message()\` 方法是这一层的实现：它把多条历史消息压缩为摘要。压缩过程中，语义重复的内容自然被合并——「用户是前端开发、写 React、前端工程师」会被压缩为「用户前端开发，主用 React」。

### 实际怎么选？

**最好的去重发生在召回阶段——先预防，少花钱。** 组装阶段的检测作为安全网，对话压缩作为最后防线。

在 GlassCortex 中，MMR 召回是第一道防线，对话压缩是第三道防线。中间的第二道（组装阶段去重）是缺失的——这也是为什么有时候你看 Lab 页的「上下文分区」面板会发现同一事实出现在多个分区中。

> 置信度：0.93

	\`\`\`mermaid
%% title: 图：重复信息三层防线
graph TD
    SOURCES["🔁 重复信息来源"]
    SOURCES --> SRC1["📥 召回多路返回<br/>FAISS + SQLite 返回同一事实"]
    SOURCES --> SRC2["💬 用户多轮重述<br/>同一事实多个说法"]
    SOURCES --> SRC3["⚙️ 系统指令重复注入"]

    SRC1 --> L1
    SRC2 --> L1
    SRC3 --> L1

    L1["🏗️ 第一层：召回去重（提前预防）<br/>MMR lambda=0.7 多样性控制<br/>最理想 — 零额外成本"]
    L1 --> L2["🔍 第二层：组装检测（执行中纠偏）<br/>语义相似度增量比较<br/>✅ 已实现 — deduplicate_candidates() 余弦去重"]
    L2 --> L3["🗜️ 第三层：对话压缩（事后清理）<br/>compress_message() 多轮合并<br/>最后防线 — 消耗 token"]
    L3 --> RESULT["✅ 去重后的上下文窗口<br/>三层协同 → 窗口利用率最高"]

    style SRC1 fill:#fca5a5,stroke:#ef4444,color:#7f1d1d
    style SRC2 fill:#fca5a5,stroke:#ef4444,color:#7f1d1d
    style SRC3 fill:#fca5a5,stroke:#ef4444,color:#7f1d1d
    style L1 fill:#34d399,stroke:#059669,color:#111
    style L2 fill:#fbbf24,stroke:#d97706,color:#111
    style L3 fill:#f97316,stroke:#ea580c,color:#fff
    style RESULT fill:#4f46e5,stroke:#4338ca,color:#fff
	\`\`\`

> 置信度：0.93`,
    l2: `### 各层去重的工程实现

#### 召回层：MMR 的多样化机制

\`src/memory/recall.py\` 中的 MMR 实现使用余弦相似度做多样化排序：

\`\`\`python
def mmr_rerank(
    query_embedding: np.ndarray,
    candidates: list[tuple[str, np.ndarray, float]],  # (id, emb, score)
    lambda_: float = 0.7,
    top_k: int = 5,
) -> list[tuple[str, float]]:
    selected = []
    remaining = list(candidates)
    while len(selected) < top_k and remaining:
        mmr_scores = []
        for cand_id, cand_emb, rel_score in remaining:
            if not selected:
                mmr = rel_score  # 第一轮仅看相关度
            else:
                # max similarity to already selected
                max_sim = max(
                    cosine_similarity(cand_emb, sel_emb)
                    for _, sel_emb, _ in selected
                )
                mmr = lambda_ * rel_score - (1 - lambda_) * max_sim
            mmr_scores.append(mmr)
        best_idx = np.argmax(mmr_scores)
        selected.append(remaining.pop(best_idx))
    return [(sid, sscore) for sid, _, sscore in selected]
\`\`\`

MMR 的去重效果取决于嵌入的语义粒度。如果使用的是 \`src/embed.py\` 中的通用嵌入模型（all-MiniLM-L6-v2），两条相似但意图不同的事实（如「用户喜欢 Python」和「用户用 Python 写后端」）可能被判定为重复而误删。这个粒度问题在 Phase 2 的评估中被记录为已知局限，改进方向是使用分层嵌入（事实级 + 话题级）。

#### 组装层：目前缺失

\`src/context/overflow_sim.py\` 的 \`simulate_overflow()\` 负责决定哪些记忆进上下文，但它只看 token 数量不看内容是否重复。如果在策略 Prioritize 模式下，两条高相关的记忆恰好内容重叠，它们会同时被选中进入窗口。

添加组装阶段去重需要：
1. 在 \`_apply_prioritize()\` 中引入一个去重步骤
2. 使用与召回层相同的嵌入（避免额外调用）
3. 对已选记忆做增量去重——选一条检查一条，而不是全部选完再去重

\`\`\`python
def _apply_prioritize_with_dedup(
    memories: list[dict],
    query_embedding: np.ndarray,
    window_size: int,
) -> list[dict]:
    selected = []
    selected_embs = []
    for mem in sorted(memories, key=lambda m: m["relevance"], reverse=True):
        # 与已选记忆的相似度检查
        if selected_embs:
            max_sim = max(cosine_similarity(mem["embedding"], e) for e in selected_embs)
            if max_sim > 0.92:  # 相似度阈值，可配置
                mem["dropped_reason"] = "重复：语义相似度 {:.2f}".format(max_sim)
                mem["dropped"] = True
                continue
        if sum(m["tokens"] for m in selected) + mem["tokens"] > window_size:
            break
        selected.append(mem)
        selected_embs.append(mem["embedding"])
    return selected
\`\`\`

这个改进已登记在 master-backlog 中但未排期——因为它涉及溢出模拟器的行为变更，需要先在 \`test_overflow_sim.py\` 中添加去重行为的测试用例，再改实现。

#### 压缩层：对话压缩的去重效果

\`src/chat/engine.py\` 的 \`compress_message()\` 使用 LLM 调用对历史消息做摘要。LLM 在生成摘要时会自然地合并重复信息——因为重复信息在语义上不提供额外价值，LLM 会将其过滤掉。

需要留意的是：压缩本身消耗 token（LLM 调用的输入 + 输出）。如果重复信息不多，压缩消耗的 token 可能超过节省的——这就是 q4.9 讨论的「压缩后悔」问题。GlassCortex 当前没有实现压缩 ROI 预判，始终执行压缩。

> 置信度：0.90`,
    l3: `### 重复信息处理的深层问题

**认识论的困境：什么是「重复」？**

两条信息「用户是前端工程师」和「用户主要写 React」——它们是重复吗？

从工程角度看，它们的语义相似度可能达到 0.85+（因为「前端工程师」和「React」在嵌入空间中距离很近）。但从认知角度看，第二条是第一条的补充信息而不是重复——它提供了具体的技术栈细节。**过早的去重会丢失信息「粒度」——你只知道「用户是前端工程师」，不知道「具体用什么框架」。**

这引出了一个有趣的理论问题：去重的粒度应该由谁决定？

- **任务视角**：如果是推荐 React 相关的答案给用户，「React」是关键信息，不能去重
- **画像视角**：如果是估计「用户的技术栈」，两条信息可以合并为「前端（React）」
- **预算视角**：如果 token 很紧张，宁愿保留较具体的那条

GlassCortex 目前没有实现上下文感知的去重粒度控制——这是 MMR 的 \`lambda\` 参数所不能表达的（它只控制多样性与相关度的权重，不能区分「补充信息」和「重复内容」）。

一种可行的研究方向是 **「信息增益」引导的去重**——不是问「这条信息跟已有信息像不像」，而是问「这条信息加入后，模型对用户的理解增加了多少」。信息增益可以基于：
- 已有的对话历史（在已有知识上是否提供了新的事实或限定条件）
- 当前查询的嵌入空间覆盖（是否引入了新的语义方向）
- 事实的同义归类（\`src/memory/triple.py\` 的 \`normalize_fact()\` 可以识别同一事实的不同表达）

**行业实践对比**

| 平台 | 去重策略 | 实现层级 |
|------|---------|:-------:|
| LangChain | MMR selector，默认 \`lambda=0.5\` | 召回层 |
| LlamaIndex | 节点去重 + 嵌入相似度阈值 (0.85) | 组装层 |
| Haystack | DeduplicateRanker，模糊推理 + 精排 | 召回层 + 组装层 |
| MemGPT/Letta | 递归摘要（无显式去重，靠压缩自然消除） | 压缩层 |
| **GlassCortex** | **MMR 召回 + 对话压缩，中间缺口** | **召回层 + 压缩层** |

GlassCortex 是目前唯一在召回层和压缩层之间留有去重缺口的方案。LangChain 和 LlamaIndex 都在组装层做了显式去重，而且它们使用的是相同的嵌入模型——说明这不是技术限制，而是「还没做」。

**已实现**：在召回阶段，\`deduplicate_candidates()\` 在 MMR 重排前执行语义去重（余弦相似度阈值 0.92），\`mmr_rerank()\`（默认 λ=0.7）提供多样性重排。两者在 \`RecallEngine.recall()\` 中自动串联。更精细的增量去重（组装层实时比较新召回与已注入内容）和 Truncate 模式下保持零开销是远期增强。

> 置信度：0.87`,
    labLinks: [{ tab: "data", label: "记忆浏览与去重分析" }],
  },
  {
    id: "q1.4",
    question: '如何处理上下文窗口中的噪声信息？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P1",
    confidence: { l0: 0.95, l1: 0.92, l2: 0.88, l3: 0.85 },
    overallConfidence: 0.85,
    l0: '噪声信息就是那些占窗口但不提供价值的内容——像开会时有人一直说废话，听着烦、记不住、还挤占真正有用的信息。处理手段有三道关：在召回时用置信度+相关度双筛拦住低质量结果、在组装时用信号检测剔除偏离当前话题的内容、在训练/微调侧引导模型学会忽略无关信息。',
    l1: `桌面上那 10 张便签不仅是数量有限的问题——如果其中有几张写的是「今天天气不错」、「隔壁工位换了新键盘」、「楼下咖啡店买二送一」，你就需要用珍贵的位置来存放这些跟手头工作毫不相干的内容。

这就是**噪声信息**在上下文窗口中的体现。它不触发溢出（因为总量可能没超）、也不是重复（内容本身是独特的）——但它占用了有限的注意力资源，让你的 AI 助手在处理真正关键的信息时「分心」。

### 噪声从哪来？

**来源一：记忆召回的「假阳性」**

这是最常见的来源。当 \`src/memory/recall.py\` 做语义搜索时，FAISS 索引返回的是嵌入空间中最接近的 Top-K 结果——但「语义接近」不等于「相关信息」。比如用户问「Python 的列表推导式怎么写」，如果对话历史里有「昨天用 Python 画了个饼图」和「Python 的 PEP 8 规范」，后者显然更相关，但嵌入空间的「距离」可能让前者分数更高，因为它在讨论 Python 的具体功能。

这种假阳性召回在每条单独看来都不算出错（确实跟 Python 有关），但多条堆在一起就成了噪声——每个都跟当前话题有一点点关系，但没有一条提供了真正的价值。

**来源二：系统指令的冗余注入**

多轮对话中，系统指令（system prompt）每轮都会注入。如果系统指令包含固定的角色设定、规则列表、输出格式要求，这些内容在每轮的 top-k 上下文中都占据位置。虽然每条只占几十 token，但累加多轮就成了背景噪声——模型看了几百次「你是一个 AI 助手」，但这条信息在第一轮之后就完全没有信息量了。

**来源三：多路召回的重叠噪音**

当 \`recall.py\` 同时从 FAISS 索引和 SQLite 召回时，不同索引返回的结果可能覆盖不同角度——但更常见的情况是：一批中度相关的记忆大量涌入，彼此之间没有重复（所以 MMR 不会过滤它们），但整体上偏离了当前查询的核心意图。

### 三道防线

**第一道：召回阶段的信号筛选**

在召回阶段不只是查「谁最相似」，还要查「谁值得被放进窗口」。GlassCortex 目前的实现里，每条召回的记忆都带有 \`composite_score\` 字段——一个综合了相关性、时效性和置信度的分数。但现实中，这个分数很少被用作硬阈值过滤：

\`\`\`python
# 当前行为：取 Top-5，不管分数多低
# 理想行为：
MIN_SCORE = 0.3  # 置信度下限
filtered = [m for m in memories if m.get("composite_score", 0) >= MIN_SCORE]
\`\`\`

加一个置信度下限就能拦住最明显的噪声——那些「好像有点关系，但分数不到 0.3」的记忆。

**第二道：组装阶段的偏离检测**

在构造上下文时，对每条候选内容做「与当前查询的偏离度」检查。这比简单的相关度排序更精细——相关度的反面不是「不相关」，而是「虽然相关但分散注意力」。

实现思路：
1. 对当前查询和候选内容分别提取关键词或嵌入
2. 计算语义距离（不是方向，而是话题的「离散程度」）
3. 如果候选内容的方向与已有内容的方向显著不同，降低其优先级

这类似于信息检索中的 **Maximal Marginal Relevance (MMR)**，但 MMR 关注的是多样性（不让内容太相似），而这里关注的是**聚焦度**（不让内容太发散）。

**第三道：模型侧的注意力引导**

最彻底的方案——不让模型看到噪声。但这其实做不到——因为你无法提前知道一个信息是「噪声」还是「关键线索」。更实际的做法是：在 prompt 中明确引导模型忽略不相关的内容。

GlassCortex 的系统提示词中包含以下指令来应对噪声：

> 如果召回的记忆与当前问题不相关，请忽略它们。优先使用对话历史中的最新信息。

这本质上是个「软过滤」——让模型自己判断哪些是噪声、哪些不是。它不如硬过滤可靠（模型有时会错误地忽略重要信息），但作为第二道防线的补充很有效。

> 置信度：0.92`,
    l2: `### 噪声检测的工程方法

#### 方法一：置信度分箱

每条召回记忆的 \`composite_score\` 实际上是一个混合了多种信号的分数。在 GlassCortex 中：

\`\`\`python
# src/memory/recall.py 中的 composite_score 构成
composite = (
    0.4 * similarity        # 语义相关度
    + 0.3 * confidence      # 事实置信度
    + 0.2 * recency         # 时效性（越近越高）
    + 0.1 * importance      # 重要性评分
)
\`\`\`

不同分段的记忆在上下文中的「预期信息密度」不同：

| 分数段 | 标签 | 预期价值 | 推荐处理 |
|:------:|:----:|:--------:|:--------:|
| ≥0.7 | 高信号 | 高度相关，高置信度 | 完整保留 |
| 0.4–0.7 | 中信号 | 相关但可能冗余或部分噪声 | 有条件保留（数量限制） |
| 0.2–0.4 | 弱信号 | 边缘相关，噪声风险高 | 仅在窗口充裕时保留 |
| <0.2 | 噪声 | 大概率无价值 | 丢弃 |

当前 GlassCortex 没有实现分箱逻辑——所有召回一视同仁地进入上下文。远期 Token 预算机制规划了基于信号质量的动态配额。

#### 方法二：话题聚焦度检测

一个更精细的方法：不只看「单条记忆跟查询的相关度」，而看「整个候选集的语义离散度」：

\`\`\`python
def compute_topic_focus(memories: list[list[float]]) -> float:
    """计算所有候选记忆嵌入的中心。离散度 = 平均到中心的距离。"""
    center = np.mean(memories, axis=0)
    distances = [np.linalg.norm(emb - center) for emb in memories]
    return float(np.mean(distances))  # 越大表示话题越发散
\`\`\`

如果离散度超过阈值（比如余弦距离 > 0.4），说明候选集已经在偏离核心话题——其中的「离群记忆」就是噪声候选。

这个方法的优势在于它不需要预设「什么算相关」——它只是检测「这一批内容在话题上是否一致」。如果用户本来就是问了一个多面性问题（比如「Python 的优势和劣势」），离散度自然高，这种情况下不应过滤。

#### 方法三：信息熵估算

从信息论角度，噪声可以用「新增信息量」来度量。如果一条记忆加入后，模型对当前问题的理解熵值几乎没有降低，那它就是噪声。

这在实际系统中很难精确计算（需要知道模型的内部分布），但可以用近似方法：

1. **压缩率测试**：用轻量级压缩算法（如 gzip）分别压缩「已选内容」和「已选内容 + 候选」，比较压缩率变化
2. **预测不确定性**：用一个小模型预测当前查询的答案，对比加入候选内容前后的预测变化
3. **词汇重叠率**：候选内容与已选内容的 n-gram 重叠比例，越高噪声可能性越大

GlassCortex 目前没有实现这些方法——它们属于 L3 的远期研究范围。

#### 与去重（q1.3）的区别

需要注意的是，噪声处理与重复信息处理（q1.3）在语义上是不同的：

- **重复**：同一信息以不同表达出现，信息增益 = 0
- **噪声**：信息本身是新的（非重复），但信息增益趋近于 0——因为它偏离了当前任务
- **区别检测**：重复的相似度通常 > 0.90，噪声的相关度可能在 0.3–0.7 之间，混淆的主要是「中等相关但实际无用的内容」

> 置信度：0.88`,
    l3: `### 噪声信息的深层问题

**问题 1：噪声的「相对性」困境**

一条信息是不是噪声，取决于当前任务——而不是信息本身。这是个根本性的两难：

- 「Python 的包管理工具 pip 和 conda 对比」——如果用户正在问 Python 列表推导式，这是噪声；如果用户正在搭开发环境，这是关键信息
- 但系统在构造上下文时不知道用户**接下来会问什么**

这意味着从工程角度，你永远无法在上下文组装阶段就做出完美的噪声过滤决策——你只能基于当前已有的信息做概率判断。而「概率判断」意味着永远会有两类错误：把有用信息当成噪声丢掉（假阳性），和把噪声当成有用信息保留（假阴性）。

**问题 2：注意力隐藏成本**

一条中度相关的噪声信息被保留在窗口中，不止是浪费 token——它还有注意力「隐藏成本」。Transformer 的自注意力机制会对窗口中所有 token 做两两交互：噪声 token 与有用 token 之间的注意力权重虽然理论上可以被模型学得忽略，但实际上会分散注意力分布，降低对关键 token 的关注度。

2023 年的研究表明，在 8k 窗口中加入 2k 的无关噪声，模型在关键问答任务上的准确率平均下降 12-15%（Liu et al., "Lost in the Middle", 2023）。而且噪声的位置很关键——窗口中间的内容受到的干扰最大，两端（开头和结尾）受影响较小。这意味着：**噪声造成的损失不只是它占用的空间，还包括它对周围内容的「注意力污染」。**

**问题 3：自适应噪声阈值**

不同任务对噪声的容忍度不同：
- **事实检索任务**（「用户的邮箱地址是什么」）：容忍度极低，一条噪声可能淹没正确答案
- **创意生成任务**（「写一首关于 AI 的诗」）：容忍度很高，甚至从中受益（噪声=灵感）
- **分析任务**（「对比两种方案的优劣」）：中间地带，需要足够的信息广度但不希望偏航

自适应噪声阈值意味着：根据当前任务的类型动态调整过滤强度。GlassCortex 的 Planner（\`src/planner.py\`）已经对用户意图做了 5 类分类——分类结果可以作为自适应阈值的输入。

**自适应阈值的设计思路：**

| 意图类别 | 噪声容忍度 | 策略建议 |
|:--------:|:----------:|:--------:|
| 事实查询 | 低 | 置信度下限 0.5，严格话题聚焦 |
| 分析推理 | 中 | 置信度下限 0.3，允许一定发散 |
| 创意生成 | 高 | 置信度下限 0.1，鼓励多样性 |
| 指令执行 | 低 | 置信度下限 0.4，只保留高相关 |
| 闲聊 | 高 | 不设限，全部保留 |

这个方案还没有在 GlassCortex 中实现——当前的 \`recall.py\` 对所有查询使用相同的召回和过滤参数。

**行业对比**

| 平台 | 噪声处理策略 | 实现层级 |
|------|-------------|:--------:|
| LangChain | relevancy_score 过滤，默认下限 0.3 | 召回层 + 组装层 |
| LlamaIndex | 节点后过滤器 + 关键词提取 | 组装层 |
| Haystack | MetaFieldFilter + QueryTimeFilter | 组装层 |
| 微软 Guidance | structured generation + 过滤约束 | 模型层 |
| **GlassCortex** | **composite_score 被动继承，无显式噪声过滤** | **召回层（仅理论）** |

> 置信度：0.85

---

\`\`\`mermaid
%% title: 图：噪声信息的三道防线
graph TD
    SOURCES["噪声来源"] --> FA["假阳性召回<br/>语义相关 ≠ 信息相关"]
    SOURCES --> SI["系统指令冗余<br/>每轮重复注入"]
    SOURCES --> MR["多路召回重叠噪音<br/>中度相关大量涌入"]

    QUERY["用户查询"] --> R1["① 召回层<br/>置信度分箱过滤"]
    R1 --> R2["② 组装层<br/>话题聚焦度检测"]
    R2 --> R3["③ 模型侧<br/>注意力引导"]

    R1 --> F1["高信号 ≥0.7 → 保留<br/>中信号 0.4–0.7 → 限数<br/>弱信号 0.2–0.4 → 条件保留<br/>噪声 <0.2 → 丢弃"]
    R2 --> F2["语义离散度检测<br/>离群记忆 → 降优先级<br/>发散度过高 → 收紧过滤"]
    R3 --> F3["系统提示引导<br/>'忽略不相关记忆'<br/>自注意力天然抑制"]

    style F1 fill:#e8f5e9
    style F2 fill:#fff3e0
    style F3 fill:#e3f2fd
\`\`\``,
    labLinks: [{ tab: "context", label: "上下文策略对比" }],
  },
  {
    id: "q1.5",
    question: '如何处理上下文窗口中的不一致信息？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P1",
    confidence: { l0: 0.95, l1: 0.92, l2: 0.88, l3: 0.85 },
    overallConfidence: 0.85,
    l0: '不一致信息比噪声更棘手——噪声只是没用，不一致是自相矛盾。当 AI 同时看到「用户喜欢简洁回答」和上次刚说过「请详细解释」时，它没有立场说「你们哪个对」。核心处理策略分三步：检测冲突（时间戳/置信度/语义对比）→ 裁决优先级（谁更新、谁更可信、谁来源权威）→ 显式标记（不强行统一，把矛盾呈现出来让用户决定）。',
    l1: `桌面上那 10 张便签不仅会过期——偶尔你会看到两张便签写了互相矛盾的内容。一张说「项目截止日周五」，另一张说「推迟到下周一」。如果你把两张都保留下来，你的「决策」就会左右摇摆：该信哪张？

LLM 面对的不一致信息比这更微妙。它不会像人类一样停下来挠头说「咦，这两条矛盾了」——相反，**它默认会尝试「统合」**，找一个中间态把矛盾圆过去。这在大多数情况下不是问题（甚至是一种优点），但当：
- 用户在多轮对话中改变了主意，
- 从不同来源召回了互相冲突的事实，
- 或者系统指令和用户输入存在隐含的矛盾指示，

这种「强行统合」就会让输出变得模糊、平滑但错误[^1]。

[^1]: 这在学术上叫「sycophancy」——模型倾向于同意最新或最权威的来源，即使它在逻辑上与其他来源矛盾。

### 不一致的三大来源

**来源一：用户跨轮次改变主意**

这是最自然也最常见的场景。用户在第一轮说「帮我找一款免费的笔记软件」，第二轮又说「预算 500 以内都行」——这两条在字面上矛盾（免费 vs. 500 以内），但 AI 应该理解这是需求的细化而非推翻。

问题在于：\`src/recall.py\` 把两轮对话都召回到上下文时，模型看到的是一条说「免费」、一条说「500」——它在生成答案时必须做选择。大多数模型会悄悄「升级」第一轮的「免费」为第二轮的「500」，然后输出一个偏题的结果。

这本身没错——但系统如果没有任何「需求变化标记」，就无法区分这种情况与下面更严重的情况。

**来源二：召回记忆之间的冲突**

更麻烦的是记忆系统内部的矛盾。当 \`src/memory/recall.py\` 从 FAISS 和 SQLite 同时召回时，可能返回两个关于同一事实但结论相反的记忆：

- 「张三的邮箱是 zhang@example.com」（置信度 0.85，3 天前录入）
- 「张三的邮箱是 san@example.com」（置信度 0.95，1 天前更新）

两条记忆语义相似度很高（都在讲张三的邮箱），所以不会被去重（q1.3）过滤。它们在主题上是「信息」（不是噪声，不会触发 q1.4 的过滤），但数值上矛盾。模型如果只看其中一条，输出就没问题；看了两条，大概率会写一个模糊的「zhang@example.com 或 san@example.com」——看似安全，实际上对用户没用。

**来源三：系统指令与用户意图的隐含冲突**

最隐蔽的不一致来源。系统指令可能包含「用中文回答」，但用户提供了英文的上下文；或者系统指令说「保持简洁」，但用户的问题要求详细推理。这种矛盾不是显式的（没有两条信息直接冲突），但在输出质量上会产生拉扯——模型会更倾向于遵循系统指令（来源优先级更高），导致输出风格或语言不匹配用户预期。

### 处理三步法

**第一步：冲突检测**

不是所有不一致都需要处理——系统不可能也不应该对每条内容做矛盾检查。触发检测的信号：

1. **语义高相似但结论相悖**：两条内容的核心实体相同（都是「张三的邮箱」），但属性值不同
2. **时间戳冲突**：同一主题的两条信息，时间戳相差较大，且属性不一致
3. **置信度梯度异常**：两条记忆的置信度差距大（0.95 vs. 0.50），且在关键属性上矛盾

**第二步：裁决优先级**

一旦检测到冲突，按下述优先级裁决：

| 条件 | 优先选择 | 理由 |
|:----:|:--------:|:----:|
| 时间戳相差 >1h | 时间戳更新的 | 多数场景下新信息覆盖旧信息 |
| 置信度相差 >0.2 | 置信度更高的 | 来源更可信的信息优先 |
| 来源层级不同 | 记忆→记忆 > 记忆→对话历史 | 对话历史的时效性 > 长期记忆的准确性 |
| 上述均无法裁决 | 标记为「不确定」，全部保留 | 不强求统一 |

**第三步：显式标记**

这是最关键的一步——**不要叫模型自己判断哪个对**，而是把矛盾显式呈现。在上下文注入时，对矛盾信息加上冲突标记：

> [冲突: 邮箱地址] 旧(zhang@example.com, 3天前) → 新(san@example.com, 1天前, 更高置信度)

这样模型在生成答案时知道这不是一个「非此即彼」的选择，而是一个有上下文的变更。它可以输出「根据最新记录，张三的邮箱是 san@example.com（3 天前的 zhang@example.com 已过时）」。

> 置信度：0.92`,
    l2: `### 不一致检测的工程方法

#### 方法一：时间戳链式比较

最简单且工程上最可靠的方法。每条记忆或消息都带时间戳，对同一主题的内容按时间排序后做相邻比较：

\`\`\`python
# 伪代码：时间链冲突检测
def detect_contradiction(records: list[dict]) -> list[Conflict]:
    conflicts = []
    # 按实体分组
    groups = group_by_entity(records)  # 提取主题实体后分组
    for entity, group in groups.items():
        sorted_grp = sorted(group, key=lambda r: r["timestamp"])
        for i in range(len(sorted_grp) - 1):
            a, b = sorted_grp[i], sorted_grp[i + 1]
            # 比较核心属性
            if key_attributes_differ(a, b) and time_gap > threshold:
                conflicts.append(Conflict(
                    entity=entity,
                    old_value=extract_value(a),
                    new_value=extract_value(b),
                    timestamps=(a["timestamp"], b["timestamp"]),
                    confidences=(a.get("confidence", 0.5), b.get("confidence", 0.5))
                ))
    return conflicts
\`\`\`

**关键参数**：时间差阈值（1 小时以内通常视为同一轮对话的修正，不触发冲突）、属性变化粒度（只有「核心属性」变化才标记——「中间名 optional → required」值得标记，「备注字段更新」不值得）。

#### 方法二：置信度 + 来源加权裁决

当时间戳无法决出胜负（比如同一轮召回的两条记忆）时，进入置信度裁决：

\`\`\`python
def resolve_by_confidence(a: dict, b: dict) -> dict:
    """基于复合置信度裁决两条矛盾信息"""
    score_a = (
        a.get("confidence", 0.5) * 0.6      # 事实置信度权重最高
        + a.get("recency_score", 0.5) * 0.2  # 时效性
        + SOURCE_WEIGHTS.get(a.get("source", "memory"), 0.5) * 0.2  # 来源权重
    )
    score_b = (
        b.get("confidence", 0.5) * 0.6
        + b.get("recency_score", 0.5) * 0.2
        + SOURCE_WEIGHTS.get(b.get("source", "memory"), 0.5) * 0.2
    )
    # 分数差 < 0.1 → 无法裁决，标记冲突
    if abs(score_a - score_b) < 0.1:
        return {"resolution": "conflict", "conflict_mark": True}
    return b if score_b > score_a else a
\`\`\`

这里的 **SOURCE_WEIGHTS** 定义各类来源的权威性：

| 来源 | 权重 | 说明 |
|:----:|:----:|:------|
| 系统指令 | 0.9 | 最高权威，通常不应被冲突 |
| 用户当前轮 | 0.8 | 用户正在说的，优先级高 |
| 用户历史轮 | 0.6 | 用户过去说的，可能有变化 |
| 记忆（高置信度） | 0.7 | 经过验证的长期记忆 |
| 记忆（低置信度） | 0.4 | 置信度不足，作为参考而非依据 |

#### 方法三：语义矛盾检测

更高级的方法——不依赖结构化属性比较，而是做语义层面的矛盾检测：

1. 提取两条候选内容的「核心主张」（claim），用小型 NLI（自然语言推理）模型判断是否矛盾
2. 如果矛盾，进一步判断是「直接矛盾」（A 说 X, B 说 ¬X）还是「间接矛盾」（A 说 X, B 隐含地否定 X）
3. 标注矛盾类型，供裁决优先级参考

这个方法在 GlassCortex 中更适合做远期方向——目前项目不包含 NLI 模型依赖。

#### 与去重（q1.3）、噪声（q1.4）的区别

三种「信息污染」的边界：

| 维度 | 重复 (q1.3) | 噪声 (q1.4) | 不一致 (q1.5) |
|:----:|:-----------:|:-----------:|:-------------:|
| 信息增益 | 0 | ≈0 | 负（误导） |
| 检测方法 | 文本相似度 | 主题偏离度 | 属性/语义矛盾 |
| 处理动作 | 合并/删除其一 | 过滤/降权 | 裁决/标记冲突 |
| 后果 | 浪费 token | 注意力分散 | 输出矛盾/模糊 |

三者在实际系统中经常交织出现：一条候选可能既是噪声（偏离当前话题）又包含不一致信息（与另一条记忆矛盾）。处理时建议按「去重→噪声→不一致」的顺序流水线处理，每条信息依次经过三层过滤器。

> 置信度：0.88`,
    l3: `### 不一致信息处理的深层问题

**问题 1：AI 的「讨好型人格」困境**

这是最根本的问题——LLM 被训练成「helper」，它天然倾向于同意对方、不引发矛盾。当上下文包含不一致信息时，模型不会像人类一样说「这两条矛盾了」，而是尝试找到一个**概括性表述**来涵盖两者。

例如，面对「张三用 Mac」和「张三用 Windows」两条矛盾记忆，模型可能输出「张三对操作系统有广泛的实践经验」——技术上「正确」（不否认任何一条），但信息上完全没有用。这叫做 **evasive generalization**（逃避性概括）。

这个问题没有完美的工程解决方案——你无法通过 prompt 工程完全消除模型的 sycophancy 倾向。最实际的做法是**在上下文注入阶段就解决矛盾**，不让模型接触到未经标记的冲突。

**问题 2：Planner 的主动澄清请求**

一个更主动的方案：当冲突检测系统发现无法通过置信度/时间戳裁决的矛盾时，不是自行决定保留哪条，而是**触发 Planner 的主动澄清**——让 AI 向用户提问。

\`\`\`python
# 主动澄清的触发条件
def should_ask_clarification(conflict: Conflict) -> bool:
    # 满足以下任一条件就主动问用户
    if conflict.score_diff < 0.1:   # 置信度无法裁决
        return True
    if conflict.entity_type == "critical":  # 关键实体（邮箱/电话/地址）
        return True
    if conflict.recurrences > 2:    # 同一实体多次矛盾
        return True
    return False
\`\`\`

主动澄清的时机很关键——不是一检测到冲突就问（那会频繁打断用户），而是等碰撞到「需要该信息的决策点」时才提问。GlassCortex 的 Planner 目前不包含主动澄清逻辑——这是一个待实现的能力。

**问题 3：不一致信息的「时效窗口」**

不是所有不一致都是错误。用户今早说「Java 是我的主力语言」，下午说「最近在学 Rust」——这两条在字面上是「主力语言不同」的矛盾，但在时间维度上是一条演化链：用户先主攻 Java，后来转向 Rust。

这种「时间窗口内的演进」应该被保留而非裁决——系统需要区分「矛盾」和「变化」：

- **矛盾**：同一时间点的两条信息对同一事实给出不同描述
- **变化**：不同时间点的同一属性自然演进（是事实更新，不是错误）

区分的核心是看时间戳对齐程度——如果两条信息的时间戳非常接近（同一轮对话/同一 session），却给出了矛盾的信息，那大概率是真矛盾；如果相隔数轮或数天，那更可能是自然变化。这个区分在第一轮信任裁决时就应该考虑——而不是等用户发现输出过时了再改。

**行业对比**

| 平台 | 不一致处理策略 | 实现层级 |
|:----:|:-------------:|:--------:|
| LangChain | 无内置不一致检测 | N/A |
| LlamaIndex | 节点去重（不检测矛盾） | 召回层 |
| MemGPT/Letta | 记忆优先顺序 + 显式冲突标记 | 记忆层 |
| 微软 E2E 记忆架构 | 属性级时间链 + 版本化 | 存储层 |
| **GlassCortex** | **无处理——当前完全依赖模型侧 sycophancy** | **未实现** |

> 置信度：0.85

---

\`\`\`mermaid
%% title: 图：不一致信息处理流程
graph TD
    INPUT["候选信息进入上下文"] --> CHECK_DUP["q1.3 去重检测<br/>是否重复？"]
    CHECK_DUP -->|重复| DROP["丢弃"]
    CHECK_DUP -->|不重复| CHECK_NOISE["q1.4 噪声检测<br/>是否偏离主题？"]
    CHECK_NOISE -->|噪声| FILTER["过滤/降权"]
    CHECK_NOISE -->|非噪声| CHECK_INCONSISTENCY["q1.5 不一致检测<br/>是否与已有信息矛盾？"]

    CHECK_INCONSISTENCY -->|不矛盾| SAFE["安全通过<br/>注入上下文"]
    CHECK_INCONSISTENCY -->|矛盾| TIMESTAMP_CHECK["时间戳比较"]

    TIMESTAMP_CHECK -->|时间差大| TIME_WINS["时间戳优先<br/>新覆盖旧"]
    TIMESTAMP_CHECK -->|时间差小| CONFIDENCE_CHECK["置信度比较"]

    CONFIDENCE_CHECK -->|差距 >0.2| HIGH_CONF_WINS["高置信度获胜"]
    CONFIDENCE_CHECK -->|差距 ≤0.2| MARK_CONFLICT["标记为冲突<br/>加冲突标记"]

    MARK_CONFLICT --> CRITICAL_CHECK["是否关键实体？"]
    CRITICAL_CHECK -->|是| CLARIFY["Planner 主动澄清<br/>向用户提问"]
    CRITICAL_CHECK -->|否| CONTEXTUALIZE["带入上下文<br/>带冲突标记"]

    style SAFE fill:#e8f5e9
    style DROP fill:#ffebee
    style FILTER fill:#fff3e0
    style MARK_CONFLICT fill:#e3f2fd
    style CLARIFY fill:#f3e5f5
\`\`\``,
    labLinks: [{ tab: "context", label: "上下文策略对比" }],
  },
  {
    id: "q1.6",
    question: '如何处理上下文窗口中的多语言信息？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P2",
    confidence: { l0: 0.95, l1: 0.92, l2: 0.88, l3: 0.83 },
    overallConfidence: 0.83,
    labLinks: [{ tab: "context", label: "多语言 Token 分析" }],
    l0: '多语言信息在上下文窗口中面临三大问题：不同语言的 token 化效率差异巨大（中文一个字符≈1 token，英文≈0.3 token）、语言混合导致注意力分散、system prompt 的语言设定可能和用户输入语言冲突。核心对策是：用统一语言写 system prompt、按输入语言匹配回复语言、理解不同语言的 token 成本差异。',
    l1: `假设你在一家跨国公司的会议室里。会议室里贴了三种语言的通知、摆着六国语言的座次表、墙上还有各种语言的便签。不同的人走进来，有的人用英语说话，有的人用中文，有的人在 PPT 上写了日文。你怎么管理这个多语言的桌面？

LLM 面对多语言输入的处境类似。但它的「语言处理」不是在语义层面做翻译——而是从底层的 token 化（Tokenization）开始，就存在着显著的语言不公平。

### 语言的不公平：不同语言的 Token 效率

Token 化的基本原理是将文本切分为 token 序列。但不同语言在 token 化时的「压缩率」差异巨大，这会直接影响你的上下文窗口利用率。

| 语言类型 | 典型文本 | 每个 token 约对应 | token 化效率（信息密度） |
|:--------:|:--------|:---------------:|:---------------------:|
| 🇬🇧 英文 | "Hello, how are you?" | 3-4 个字母 ≈ 0.7 个单词 | 📗 高效 |
| 🇨🇳 中文 | "你好吗？" | 1-2 个汉字 | 📕 低效——每个 token 包含的信息更少 |
| 🇯🇵 日文 | "お元気ですか？" | 1-2 个字符（含假名+汉字） | 📕 低效——字符集更大，嵌入维度更稀疏 |
| 🇰🇷 韩文 | "안녕하세요?" | 2-3 个字母 | 📙 中效 |
| 🇩🇪 德文 | "Tschüss!" | 4-5 个字母 | 📗 高效——与英文同为拉丁字母体系 |

这种差异意味着：**同样的上下文窗口，写中文能容纳的信息量只有英文的 1/3 到 1/2**。如果你的应用同时服务中英文用户，要特别注意这一点——给中文用户预留的窗口上限不是给英文用户的上限。

> 例如 GlassCortex 的 \`_estimate_tokens()\`（\`src/context/overflow_sim.py:53\`）用一个简单的经验公式来估算 token 数：
>
> \`\`\`python
> cjk = sum(1 for c in text if "一" <= c <= "鿿")
> other = len(text) - cjk
> tokens = math.ceil(cjk / 4) + math.ceil(other / 3)
> \`\`\`
>
> 中文字符按 4 字符=1 token 估算，非中文按 3 字符=1 token 估算。这个公式虽然粗糙，但它反映了真实的 tokenizer 行为差异：CJK 字符因为 Unicode 编码空间大、出现频率不均衡，tokenizer 需要更多维度来覆盖它们，导致每个 token 能「装下」的 CJK 字符更少。

### 实际影响：哪些环节会被语言影响

1. **上下文预算分配**：如果 system prompt 是英文（~300 token），而用户用中文写了很长的对话历史，每轮的实际 token 消耗会比直觉估算高 2-3 倍
2. **溢出触发时机**：用中文对话的会话触发的溢出判断会早于同等长度的英文对话
3. **回复语言一致性**：system prompt 规定"回复用中文"，但用户用英文提问——模型可能混用语言或在两种语言间犹豫（表现为回复质量下降）
4. **记忆召回的语言匹配**：如果召回的记忆是英文，但当前对话是中文，混合注入上下文可能导致模型混淆信号

> 置信度：0.92`,
    l2: `### GlassCortex 的多语言处理现状

当前 GlassCortex 没有显式的多语言处理层。语言感知散落在几个不同组件中，彼此没有协调。

#### 组件一：Token 估算（最相关的逻辑）

\`_estimate_tokens()\` 在 \`src/context/overflow_sim.py:53\` 和 \`src/chat/engine.py:16\`（导入使用）中有双重实现，算法一致：

\`\`\`python
# src/context/overflow_sim.py:53-60
def _estimate_tokens(text: str) -> int:
    if not text:
        return 1
    cjk = sum(1 for c in text if "一" <= c <= "鿿")
    other = len(text) - cjk
    tokens = math.ceil(cjk / 4) + math.ceil(other / 3)
    return max(1, tokens)
\`\`\`

这个函数被三处使用：
1. \`ChatEngine.generate()\` 中估算用户输入和溢出前的 token 预算（\`engine.py:166-174\`）
2. 消息压缩逻辑中对比压缩前后的 token 节省（\`engine.py:268-269\`）
3. \`OverflowSim.compare_strategies()\` 中模拟不同溢出策略的表现（\`overflow_sim.py:132-254\`）

它的 CJK/非CJK 二分法是一个**足够好但不是精确**的近似——实际 DeepSeek/OpenAI tokenizer 对中文的编码并不是简单的 1/4 比例，不同 token 对中文字符的覆盖密度随模型变体而不同。但对于溢出模拟和预算预估这类场景，这个误差在可接受范围内。

#### 组件二：语言无关的核心链路

以下能力是**语言无关**的——它们不关心输入是中文还是英文：
- **FAISS 向量索引**（\`src/memory/index.py\`）：Embedding 本身是跨语言的，一个英文记忆和一个中文记忆都能在同一个向量空间中检索
- **事实抽取**（\`src/memory/fact.py\`）：抽取规则基于语义模式（主谓宾三元组），不依赖特定语言
- **意图分类**（\`src/planner/intent.py\`）：\`INTENT_CATEGORIES\` 的中文名称（"提问"/"指令"/"探索"/"闲聊"/"澄清"）是训练数据中的标注，如果输入是英文，分类质量会下降——因为 prompt 中的类别描述是中文的

#### 组件三：潜在的多语言问题点

| 模块 | 多语言风险 | 表现 | 当前状态 |
|:----:|:---------:|:----|:--------:|
| \`_estimate_tokens()\` | 精度 | CJK/非CJK 二分法对混合语言文本（如中英夹杂）精度下降 | ⚪ 接受——用于溢出模拟而非精确计费 |
| System prompt | 语言一致性 | \`base_header\` + \`closing\` 是中文字段名+中文指令，英文传入时可能 mismatch | 🟡 未处理——但实际使用以中文为主 |
| 意图分类 prompt | 输入语言兼容 | 5 类中文定义，英文输入时可能被 LLM 误分类为"探索"或"闲聊" | 🟡 未处理——需要双语言 prompt 或自动检测 |
| Memory recall | 混合注入 | 英文记忆 + 中文当前对话 = 上下文混杂，模型注意力被稀释 | 🟡 未处理——无语言过滤，所有召回一律注入 |
| 回复语言控制 | 缺乏约束 | 没有"用户的系统 prompt 语言/回复语言"配置项 | 🔴 未实现——靠模型自主选择，无法保证一致性 |

### 工程建议：渐进式的多语言治理

如果要为 GlassCortex 增加多语言支持，推荐的实施顺序（按影响从低到高）：

1. **Prompt 双语言化**（1 天）：意图分类和事实抽取的 prompt 从中文单语改为双语（instruction in English + examples in both languages），覆盖英文输入场景
2. **语言偏好配置**（1 天）：在 \`Settings\` 中增加 \`preferred_language\` 字段，影响回复语言和记忆召回的语言过滤
3. **回复语言检测**（2 天）：在 \`ChatEngine.generate()\` 输出中加入 language code 字段，监控模型是否按预期语言回复
4. **记忆语言标记**（3 天）：在记忆的 metadata 中加入 detected_language 字段，支持按语言过滤召回结果
5. **多语言 token 预算**（5 天）：根据检测到的用户输入语言，动态调整上下文窗口的分区配比（中文用户给更大的 history 占比，因为同样的信息需要更多 token）

> 置信度：0.88`,
    l3: `### 多语言上下文管理的深层挑战

**挑战 1：Tokenizer 的公平性问题**

这不是 GlassCortex 的问题——它是所有 LLM 的底层问题。现代 tokenizer（无论是 OpenAI 的 cl100k_base、Anthropic 的还是 DeepSeek 的）都是通过大量英文文本训练出来的 BPE 分词器。这意味着英文文本的分词效率天然更高（因为训练数据中英文占比最大），而非英文文本——尤其是 CJK 字符——需要被切分成更多的 token。

具体数据：

| 模型 | Tokenizer | 1000 英文单词 | 1000 中文字符 | 比例 |
|:----:|:--------:|:------------:|:------------:|:----:|
| GPT-4 | cl100k_base | ~750 token | ~1800 token | 1 : 2.4 |
| Claude 3 | Anthropic BPE | ~700 token | ~1700 token | 1 : 2.4 |
| DeepSeek V2 | DeepSeek tokenizer | ~730 token | ~1750 token | 1 : 2.4 |

这意味着：如果某个中文用户的对话历史已经占满了 64k 窗口，切换到英文用户时同样的历史内容可能只需要 27k token 就能装下。反过来，同样的问题，中文用户需要付出的 token 成本是英文用户的 2.4 倍。

**挑战 2：窗口内的语言域隔离**

多语言输入的一个微妙问题是：**模型在生成回复时可能因为上下文中的语言混杂而产生特征泄露**。具体来说：

- \`En\` → \`Zh\` 泄漏：上下文中如果有一段中文对话，英文提问也可能得到部分中文回复
- \`Zh\` → \`En\` 泄漏：中文提问但上下文中英文占多数，回复中可能混入英文术语和非自然的语码转换

解决这个问题需要显式的"语言域标记"——在 system prompt 中明确声明当前会话的语言偏好，并在每段上下文中标注语言域。GlassCortex 当前的 \`_build_system_prompt()\` 只有 \`base_header\` 中的简单语言声明（如果有的话），没有语言域隔离机制。

**挑战 3：跨语言记忆召回中的语义漂移**

当你用英文的记忆去回答一个中文的问题时，存在语义漂移风险——同一概念在不同语言的 embedding 空间中可能处于不同位置：

\`\`\`
英文 embedding:   "memory management" → [0.23, -0.45, 0.78, ...]
中文 embedding:   "内存管理"       → [0.21, -0.42, 0.75, ...]
日文 embedding:   "メモリ管理"     → [0.19, -0.40, 0.72, ...]
\`\`\`

虽然跨语言的 embedding 在同一声明空间中距离不远（因为训练数据中会混入平行语料），但精细度会下降——比如「内存管理」的三种语言版本在向量空间中的距离，可能小于「内存管理」和「文件管理」的同语言距离。这意味着跨语言召回会增加误召回率。

### 行业对比

| 平台 | 多语言策略 | Tokenizer 类型 | 语言检测 | 按语言分区 |
|:----:|:---------:|:------------:|:--------:|:---------:|
| OpenAI API | 单 tokenizer 覆盖百种语言 | cl100k_base (BPE) | 无 | 无——所有语言统一处理 |
| Anthropic Claude | 同上 | Anthropic BPE | 无 | 无 |
| Cohere | 多语言 embedding 优化 | 多语言专用 tokenizer | 有（语言分类器） | 有（按语言路由） |
| Google Gemini | 多语言预训练均衡 | SentencePiece (Unigram) | API 层支持 language param | 有（用户可指定语言偏好） |
| Llama 3.1 | 多语言但英文最优 | tiktoken BPE | 无 | 无 |
| **GlassCortex** | **中文为主，英文可兼容** | **启发式 CJK/non-CJK 估算** | **无** | **无** |

\`\`\`mermaid
%% title: 图：多语言上下文中的 Token 效率差异与影响链路
graph TD
    LANG["🌐 用户输入语言"] --> DETECT{"语言检测"}
    DETECT -->|"中文"| ZH["中文文本"]
    DETECT -->|"英文"| EN["英文文本"]

    ZH --> TOK["Tokenizer 化"]
    EN --> TOK

    TOK --> ZHTOK["中文：~1.8× token"<br/>每个字符约 1.4-2.0 token"]
    TOK --> ENTOK["英文：~1.0× token"<br/>每个单词约 0.7-0.8 token"]

    ZHTOK --> BUDGET["上下文预算分配"]
    ENTOK --> BUDGET

    BUDGET --> OVERFLOW["溢出触发时机不同"]
    BUDGET --> COST["Token 成本差异"]
    BUDGET --> COMPRESS["压缩策略效果不同"]

    SYSTEM["System Prompt 语言"] --> CONSISTENCY{"语言一致性<br/>检查"}
    CONSISTENCY -->|"一致"| SMOOTH["🚀 流畅回复"]
    CONSISTENCY -->|"冲突"| CONTAM["⚠️ 语言域泄漏<br/>中英混杂"]
    CONSISTENCY -->|"未设置"| MIXED["❓ 模型自主选择<br/>不可控"]

    MEMORY["多语言记忆召回"] --> EMBED["Cross-lingual Embedding"]
    EMBED --> DISTANCE["同概念跨语言距离 ≈ 0.05"]
    EMBED --> SAME_LANG["同语言同概念距离 ≈ 0.02"]
    EMBED --> FALSE_RECALL["误召回率上升 ~2-3×"]

    style LANG fill:#818cf8,stroke:#6366f1,color:#fff
    style TOK fill:#f59e0b,stroke:#d97706,color:#111
    style BUDGET fill:#34d399,stroke:#059669,color:#111
    style CONSISTENCY fill:#fca5a5,stroke:#ef4444,color:#7f1d1d
    style EMBED fill:#a78bfa,stroke:#8b5cf6,color:#fff
\`\`\`

> 置信度：0.83

---

**注释**：本节讨论的是多语言信息在**同一个上下文窗口**中的处理问题，即"不同语言的信息如何共享同一个窗口"。不讨论多语言翻译系统本身——那是另一个领域的问题。`,
  },
  {
    id: "q1.7",
    question: '如何处理上下文窗口中的多任务信息？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P2",
    confidence: { l0: 0.95, l1: 0.92, l2: 0.88, l3: 0.82 },
    overallConfidence: 0.82,
    labLinks: [{ tab: "context", label: "意图分类与多任务分析" }],
    l0: '多任务信息在上下文窗口中的核心矛盾是「注意力竞争」——不同任务的数据被不加区分地塞入同一个窗口，彼此争夺注意力。对策分三层：离线层用意图分类做任务分流、组装层按当前任务优先级排列信息、运行时层用 Token 预算和分区隔离减少任务间干扰。',
    l1: `想像一下你的书桌上同时摊开了三件事：你在写一份工作报告（任务 A），同时开着一个购物网站帮朋友参考商品（任务 B），手机上是老板发来的临时需求（任务 C）——三件事混在一起，你每看一眼书桌都要短暂切换一次上下文。

LLM 的上下文窗口在面对多任务时遭遇类似困境。窗口是一个线性序列——所有任务的数据被首尾相连地串在一起，模型在读到某段文本时并不知道这段属于任务 A 还是任务 B。如果用户先问了一个技术问题（任务 A），然后又闲聊了几句（任务 B），接着又回到技术问题（任务 A）——模型需要从窗口的线性文本中自己推断哪些内容是相关的。

### 多任务带来的核心问题

**问题一：任务间注意力污染**

当任务 A 和任务 B 的信息被不加区分地放入同一个窗口，任务的上下游信息出现在窗口不同位置，模型的注意力会被分散。举例：

> 用户前 5 轮在讨论「React 状态管理的最佳实践」（任务 A）
> 第 6 轮问了一句：「今天晚上吃什么比较好？」（任务 B——闲聊）
> 第 7 轮回到：「刚才你说的 Redux Toolkit 怎么用？」（回到任务 A）

在第 7 轮时，模型需要在窗口中定位哪段信息属于任务 A 的上下文、哪段是任务 B 的干扰。没有显式的任务边界标记，模型只能依靠语义推测——这在同一个话题内效果尚可，但跨话题切换时，任务 A 的重要信息可能因为任务 B 的出现而被注意力机制"稀释"。

**问题二：信息的内聚性被破坏**

一个理想的上下文应该服务于一个对话目标。当多任务混在一个窗口中，每条信息的"上下文价值"是由离它最近的任务决定的——而不是由它原始所属的任务决定。这使得召回阶段的"相关度排序"不可靠：一条关于 "Redux 中间件" 的记忆可能因为附近有闲聊内容而被降权。

**问题三：溢出策略的选择被扭曲**

当需要截断时，溢出策略（FIFO / 优先级 / 摘要压缩）面对混合任务都很棘手——你无法确定一条消息是"旧的但仍属于当前活跃任务"还是"任务 B 的残留痕迹"。错误的截断可能把当前活跃任务的关键上下文切掉，而保留无用的历史。

> 置信度：0.92`,
    l2: `### GlassCortex 的多任务处理架构

GlassCortex 没有显式的多任务管理层，但它的 Planner 子系统提供了一种隐式的多任务处理机制——通过意图分类（Intent Classification）来区分不同任务类型，并在后续处理中赋予不同的优先级和行为。

#### 层级一：任务识别——PlannerEngine 意图分类

\`src/planner/intent.py\` 的 \`PlannerEngine.classify_intent()\` 是 GlassCortex 多任务处理的起点：

\`\`\`python
# src/planner/intent.py:21-24
INTENT_CATEGORIES = ("提问", "指令", "探索", "闲聊", "澄清")

# src/planner/intent.py:159-190（简化）
def classify_intent(self, user_msg: str) -> tuple[IntentResult, dict[str, object]]:
    """将用户消息分类为 5 种意图之一，附带置信度和原始 LLM 响应。"""
    prompt = self._build_intent_prompt(user_msg)
    response = self.llm.chat([{"role": "user", "content": prompt}])
    category, confidence = self._parse_response(response)
    result = IntentResult(
        category=category,
        confidence=confidence,
        raw_response=response,
        timestamp=datetime.now(),
    )
    return result, {"prompt": prompt, "raw_response": response}
\`\`\`

五种意图类型实际上对应了五种"任务类别"：

| 意图类别 | 对应的任务类型 | 典型 Query 示例 | 对话历史保留策略 |
|:--------:|:------------:|:--------------:|:--------------:|
| 提问 (Query) | 知识检索/问答 | "什么是上下文窗口？" | 保留相关历史辅助理解 |
| 指令 (Instruction) | 系统操作/推理 | "帮我把上段对话压缩成摘要" | 保留操作目标相关的上下文 |
| 探索 (Explore) | 开放式讨论 | "你说说 Token 还有什么有趣的点？" | 低历史依赖——不需要太多上下文 |
| 闲聊 (Chatter) | 社交/无关内容 | "今天天气不错" | 最小历史保留——避免污染后续任务 |
| 澄清 (Clarify) | 纠错/补充信息 | "我不是这个意思，重新解释一下" | 需要回退到被澄清的那段上下文 |

意图分类之后，\`ChatEngine.generate()\` 会根据意图类别影响 Token 预算分配：

\`\`\`python
# src/chat/engine.py:166-174（简化）
user_tokens = _estimate_tokens(user_input)
dropped_token_sum = sum(_estimate_tokens(d) for d in dropped)
context_meta["user_message_tokens"] = user_tokens
context_meta["total_estimated_tokens"] = (
    base_tokens
    + memories_token_before
    - dropped_token_sum
    + user_tokens
)
\`\`\`

但当前实现并未在系统层面对不同意图施加不同的预算策略——所有意图共享同一个溢出处理流程。意图分类的结果主要用于前端展示（ProcessDrawer 中的 Intent pill 标签），而不是用于引导上下文组装策略。

#### 层级二：任务隔离——当前缺失的关键环节

GlassCortex 缺少的任务隔离机制包括：

| 缺失机制 | 问题 | 建议实现 |
|:--------:|:----|:--------|
| 任务边界标记 | 不能区分上下文窗口中的哪段属于哪个任务 | 每条上下文消息附加 \`task_id\` 元数据，用 \`ContextPartitions\` 做任务级分区统计 |
| 任务切换检测 | 没有检测用户「从任务 A 切到任务 B」的时刻 | 在 \`PlannerEngine\` 中增加 \`detect_switch()\` 方法——比较本次和上次意图类别，差异≥2 类（如"提问→闲聊"）触发切换信号 |
| 每任务历史窗口 | 截断策略不知道哪段历史属于哪个任务 | 引入 \`TaskHistoryRing\`——每个任务独立维护一个环形缓冲区，溢出只截断本任务的旧内容 |
| 任务优先级调度 | 没有机制在窗口拥挤时确定哪个任务信息优先保留 | 引入任务优先级（派生自意图类别：指令 > 提问 > 澄清 > 探索 > 闲聊），低优先级任务的历史优先被截断 |

#### 层级三：Planner 链中的任务关联

\`src/planner/intent.py\` 在更高阶的规划流程中提供了多任务关联的能力：

\`\`\`python
# src/planner/plan.py — PlanGenerator 的任务分解
# src/planner/replan.py — ReplanDetector 的重新规划检测
# src/planner/reflection.py — ReflectionEngine 的反思回环
\`\`\`

这三个高阶组件（\`PlanGenerator\`、\`ReplanDetector\`、\`ReflectionEngine\`）在 \`src/bootstrap.py:173-186\` 中初始化并注册到 \`PlannerEngine\`：

\`\`\`python
# src/bootstrap.py:173-186
from src.planner.plan import PlanGenerator
from src.planner.reflection import ReflectionEngine
from src.planner.replan import ReplanDetector
plan_gen = PlanGenerator(store, idx)
planner.set_plan_generator(plan_gen)
replan_detector = ReplanDetector(store)
planner.set_replan_detector(replan_detector)
reflection_engine = ReflectionEngine(store)
planner.set_reflection_engine(reflection_engine)
\`\`\`

它们提供了一个「任务生命周期」的完整视图——从意图分类（任务识别），到计划生成（任务分解），到执行中的重规划检测（任务切换/暂停），到执行后的反思（任务完成度评估）。但这个链条目前是线性的——一次只能处理一个任务的完整生命周期。多任务并行时，这些组件之间没有协调机制。

> 置信度：0.88`,
    l3: `### 多任务上下文管理的深层挑战与演进方向

**挑战 1：上下文窗口的「线性叙事」限制**

Transformer 的注意力机制本质上是一个线性序列处理器——它没有对任务边界的内置感知。位置编码（Rotary Position Embedding / RoPE）只编码 token 在序列中的位置，不编码 token 属于哪个"段"或哪个"任务"。

一个直观的对比：

| 人类认知 | LLM 上下文窗口 |
|:--------:|:-------------:|
| 多桌面工作空间（你可以在不同桌面放不同任务） | 单线性序列（所有任务的数据串在一起） |
| 任务切换时有显式的「翻页」动作 | 任务间没有任何分隔符（除非手动插入特殊 token） |
| 可以关掉一个任务的工作空间暂不处理 | 没有"软隔离"——所有已注入的信息都参与注意力计算 |
| 记忆与决策在不同任务间有自然的衰退曲线 | 注意力分布由所有 task 的内容共同竞争 |

**挑战 2：Segmented Attention 的学术探索**

学术界已经在探索为多任务设计注意力机制的变体：

- **Segment-Level Attention**：在注意力计算中加入段级编码。每段上下文被标记归属于哪个任务，注意力权重在计算时按段分组
- **Task-ID RoPE**：在位置编码中注入任务 ID 信息，使模型能区分不同任务的空间位置
- **Adaptive Sparse Attention**：根据任务热力图动态屏蔽不相关任务的注意力路径——当前活跃任务能看到所有内容，非活跃任务只看到自己历史

GlassCortex 的 \`ContextPartitions\`（\`src/context/partition.py\`）已经开始做分区级的 Token 统计，但要扩展到任务级注意力隔离还需要底层框架的支持。

**挑战 3：任务切换时的记忆污染**

当用户从任务 A 切换到任务 B，再回到任务 A 时，有一段中间的任务 B 数据被注入到了上下文中。这段数据对任务 A 是噪声。抑制噪声的手段：

1. **显式任务标记**：每条消息附加意图类别标签，在当前处理任务 A 时降低任务 B 相关 token 的注意力权重（需要模型支持 attention mask 定制）
2. **任务级消息折叠**：在溢出处理时，将任务 B 的历史折叠为一行摘要（"期间用户聊了一些关于吃饭的内容"），保留上下文完整性同时压缩
3. **自动回退点检测**：如果检测到用户回到任务 A，自动回退到任务 A 最后的上下文状态（类似于 git checkout——切换分支时恢复对应工作区状态）

### 行业对比

| 平台 | 多任务处理方式 | 任务隔离级别 | 动态性 |
|:----:|:------------:|:----------:|:------:|
| ChatGPT（默认） | 无显式多任务管理，模型靠语义自行处理 | 无隔离 | 完全被动 |
| ChatGPT（Custom Instructions） | 静态规则——在 system prompt 中声明任务类型 | 指令级 | 静态——一次设定全场生效 |
| Claude Projects | 通过 Project Knowledge 文件隔离不同任务上下文的注入 | 文件级 | 按用户手动切换 |
| LangChain | Agent 每次执行时根据意图选择工具链，链间无上下文污染 | 链级 | 动态——每条链独立 |
| AutoGPT | 每个目标有独立的内存空间，长期存储中自动聚类 | 目标级 | 动态——目标创建/销毁时自动维护 |
| MemGPT / Letta | 按主题自动聚类记忆，每条记忆带标签，检索时过滤 | 标签级 | 动态+自适应 |
| **GlassCortex** | **Planner 意图分类 + 无显式隔离** | **消息级**（有分类无调度） | **半自动**（分类独立运行，未用于上下文组装） |

\`\`\`mermaid
%% title: 图：多任务上下文中的注意力竞争与隔离策略
graph TD
    USER["👤 用户多轮输入"] --> CLS{"PlannerEngine<br/>classify_intent()"}

    CLS -->|"意图 A"| TA["任务 A 上下文"]
    CLS -->|"意图 B"| TB["任务 B 上下文"]
    CLS -->|"意图 C"| TC["任务 C 上下文"]

    subgraph "当前状态：无隔离"
        TA --> MERGE["线性拼接<br/>所有任务数据混入同一窗口"]
        TB --> MERGE
        TC --> MERGE
        MERGE --> ATTN["Attention 竞争"]
        ATTN --> CONTAM["⚠️ 任务间污染<br/>注意力被均匀稀释"]
        ATTN --> SWITCH["⚠️ 任务切换后<br/>旧任务残留噪音"]
    end

    subgraph "理想状态：任务级隔离"
        TA_TG["标记 task_id=A"] --> TA_BUF["TaskHistoryRing A<br/>独立环形缓冲区"]
        TB_TG["标记 task_id=B"] --> TB_BUF["TaskHistoryRing B<br/>独立环形缓冲区"]
        TC_TG["标记 task_id=C"] --> TC_BUF["TaskHistoryRing C<br/>独立环形缓冲区"]

        TA_BUF --> PRIORITY{"优先级调度"}
        TB_BUF --> PRIORITY
        TC_BUF --> PRIORITY
        PRIORITY --> SELECT["按优先级+活跃度<br/>选择当前注入任务"]
        SELECT --> CLEAN["🚀 干净上下文<br/>只含当前任务关键信息"]
    end

    PRIORITY -.-> RULES["优先级规则<br/>指令 > 提问 > 澄清<br/>> 探索 > 闲聊"]

    style USER fill:#818cf8,stroke:#6366f1,color:#fff
    style CLS fill:#f59e0b,stroke:#d97706,color:#111
    style MERGE fill:#fca5a5,stroke:#ef4444,color:#7f1d1d
    style CLEAN fill:#34d399,stroke:#059669,color:#111
    style PRIORITY fill:#a78bfa,stroke:#8b5cf6,color:#fff
\`\`\`

> 置信度：0.82

---

**注释**：这里讨论的是多个任务的信息在同一个上下文窗口中处理时的竞争和隔离问题。不讨论任务规划（Plan Generator / Replan Detector 的任务分解和执行能力，那是 q3.x 系列的话题），只讨论「当多个任务的信息不得不共享同一个窗口时，如何减少它们的相互干扰」。

`,
  },
  {
    id: "q1.8",
    question: '上下文组装策略：给定一个 token 预算，如何决定"塞什么、按什么顺序塞"？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P1",
    confidence: { l0: 0.95, l1: 0.92, l2: 0.88, l3: 0.85 },
    overallConfidence: 0.85,
    labLinks: [{ tab: "context", label: "上下文策略对比" }],
    l0: '上下文组装就像打包一个登机箱——预算有限，你要决定带什么（优先级）、按什么顺序放（排列策略）、放不下的怎么办（溢出处理）。核心是四步决策：先算总预算（窗口大小减输出预留）→ 再分四区配比（system/recall/history/output）→ 然后按优先级从高到低填充各分区 → 最后按"核心两端、辅助中间"的策略排列。',
    l1: `假设你要去出差，登机箱就那么大——跟上下文窗口同款烦恼。你不可能什么都带，必须决定：西装（系统指令）肯定要带，笔记本电脑（当前查询）也得放进去，充电器（相关记忆）尽量带上，换洗衣服（对话历史）能塞就塞——实在装不下就少带两件。

这个「装行李」的过程在 LLM 系统中叫**上下文组装**（Context Assembly）。它不是一次性完成的——每一轮对话都要重新组装，因为上一轮的输出变成了下一轮的历史。

### 四步组装决策

**第一步：算预算**

上下文窗口的总容量是所有输入共享的，而输出（模型的回复）必须预留空间。实际可用于输入的 token 数 = 窗口大小 - max_tokens（输出预留）。

以 DeepSeek 的 64k 窗口为例，如果设置 max_tokens=4096，实际可用输入预算就是 64k - 4k = 60k token。

**第二步：分四区**

预算分给四个区，每个区竞争同一块空间：

| 分区 | 内容 | 生命周期 | 典型预算占比 |
|:----:|:----:|:--------:|:----------:|
| System | 角色设定、规则、输出格式约束 | 每轮不变（可缓存） | 10-15% |
| Recall | 召回的长期记忆、事实、画像 | 按需注入，跨轮重复 | 30-40% |
| History | 当前会话的对话历史 | 每轮新增（最动态） | 30-40% |
| Tools | 工具定义、API 响应、函数结果 | 按需注入 | 5-10% |

这四区并不是固定配比——\`src/context/overflow_sim.py\` 的 \`compare_strategies()\` 可以模拟不同配比下的效果，但当前实现用的是「固定权重」而非「动态调整」。

**第三步：按优先级填**

每个分区内部再按优先级筛选。不是所有的召回记忆都值得进窗口，也不是所有的历史消息都该保留。优先级判断的常见维度：

1. **相关性**：跟当前查询在语义上多接近（由 recall 阶段的相似度分数决定）
2. **时效性**：最近的消息比旧消息更有价值（用户几分钟前说的比几小时前的重要）
3. **信息密度**：包含具体事实的消息比寒暄有价值（「张三的邮箱是 zhang@example.com」>「今天的天气不错」）
4. **来源权重**：系统指令 > 当前用户输入 > 对话历史 > 召回记忆（按可靠度排序）

GlassCortex 的 \`_build_system_prompt()\` 中，分区和筛选是串行执行的——先组装 system 区（完全保留），再填充 recall 区（按相关度从高到低），最后填入 history 区（从最近的对话起往回填）。如果填充过程中超出预算，就调用 \`simulate_overflow()\` 做截断或压缩。

**第四步：定顺序**

东西选好了，怎么排列？这可能是最微妙的一步——因为**排列顺序直接影响模型的注意力分配**。

研究表明（Liu et al., "Lost in the Middle", 2023），LLM 对上下文不同位置的关注度是不均匀的：

- **开头**（primacy effect）：模型对最早看到的内容关注度最高
- **中间**：注意力最低——窗口中间的信息最容易丢失
- **结尾**（recency effect）：对最近输入的内容也有较高关注度

这意味着：**越重要的内容越要放在开头或结尾，中间位置留给辅助信息**。

常见的排列策略：

| 策略 | 排列方式 | 适用场景 |
|:----:|:--------|:--------|
| System-First | 系统指令 → 召回记忆 → 对话历史 → 当前用户输入 | 标准 QA，系统指令最重要 |
| Query-Last | 召回记忆 → 对话历史 → 系统指令 → 当前用户输入 | 依赖上下文的场景，最新输入最重要 |
| Sandwich | 系统指令 + 当前输入在两端，中间放历史 | 需要兼顾系统约束和最新意图 |
| Priority-Clustering | 按优先级降序排列，高的在两端 | 信息密度不一时，核心事实最大化可见 |

GlassCortex 默认使用 System-First 策略：系统指令在最前面，当前用户输入在最后，对话历史和召回记忆在中间。这是一个各场景下表现最均衡的选择。

> 置信度：0.92`,
    l2: `### GlassCortex 的上下文组装管线

目前的上下文组装逻辑集中在 \`src/chat/engine.py\` 的 \`_build_system_prompt()\` 方法中。这不是一个独立模块——它是聊天引擎中的一个步骤，跟记忆召回、溢出模拟、回复生成等步骤耦合在一起。

**组装流程（伪代码）：**

\`\`\`python
# src/chat/engine.py — 上下文组装核心逻辑（简化）
def _build_system_prompt(self, query: str, session: SessionState) -> str:
    parts = []

    # Step 1: System 区 — 角色设定 + 规则
    system_prompt = self._get_base_system_prompt()
    parts.append(("system", system_prompt))

    # Step 2: Recall 区 — 召回的长期记忆
    memories = self.recaller.recall(query, top_k=10)
    # 按 composite_score 降序
    memories.sort(key=lambda m: m.composite_score, reverse=True)
    if memories:
        recall_block = self._format_recall_block(memories)
        parts.append(("recall", recall_block))

    # Step 3: History 区 — 当前对话历史
    history = self._format_history(session.messages[-self.max_history:])
    if history:
        parts.append(("history", history))

    # Step 4: 溢出检测 + 处理
    total_tokens = sum(count_tokens(p[1]) for p in parts)
    output_budget = self.max_tokens
    available = self.window_size - output_budget
    if total_tokens > available:
        result = simulate_overflow(parts, available, strategy=self.overflow_strategy)
        parts = result.retained_parts
        # 被丢弃/压缩的部分记录到 OverflowSimResult.dropped_items

    # Step 5: 按 System-First 策略排列
    system_block = next(p[1] for p in parts if p[0] == "system")
    non_system = [p[1] for p in parts if p[0] != "system"]
    return system_block + "\\n\\n".join(non_system) + "\\n\\n用户输入：" + query
\`\`\`

**关键观察：** 当前实现中，排列策略是硬编码的 System-First——调用 \`_build_system_prompt()\` 时没有参数可以切换排序策略。这意味着如果你想让系统切换为 Query-Last 或 Sandwich，必须修改引擎代码。

### Partition 模块的预算计算

\`src/context/partition.py\` 提供了更精细的分区预算计算。核心数据结构 \`ContextPartitions\` 记录了四个分区的 token 占用：

\`\`\`python
@dataclass
class ContextPartitions:
    system_tokens: int
    recall_tokens: int
    history_tokens: int
    tools_tokens: int
    total_tokens: int  # 总和
    window_size: int
    output_budget: int
    remaining: int  # window_size - total_tokens - output_budget
\`\`\`

\`compute_partitions()\` 函数根据各分区实际内容和窗口大小计算剩余预算，并在 \`remaining < 0\` 时触发溢出警告——但触发后具体怎么做（调用哪个策略），由 ChatEngine 决定，不是 Partition 模块的责任。

### Lost-in-the-Middle 缓解

当前代码没有实现 lost-in-the-middle 缓解。三条可行的工程方向：

**方向一：关键信息多位置备份**

把最关键的信息（如当前用户目标、核心事实）在上下文中出现两次——一次在开头，一次在结尾附近。这样即使中间部分被忽略，关键信息也能被注意到。代价是额外占用 token。

**方向二：注意力强化标记**

在关键内容前加显式的注意力标记，如 \`[重要] 当前用户的核心需求是...\` 或 \`⚠️ 以下信息对本次回答至关重要：\`。一些研究显示这种元标记可以提升 LLM 对该段落的关注度，但效果因模型而异。

**方向三：自适应位置分配**

不固定排列策略，而是根据当前内容的特点动态选择：如果召回记忆的信息密度高、分散度大（包含多个重要事实），就用 Priority-Clustering 把高密度事实放在两端；如果对话历史比召回记忆更有价值（用户刚提供了关键信息），就用 Query-Last 或 Sandwich。

GlassCortex 目前三个方向都没有实现——lost-in-the-middle 缓解是规划中的 Token 预算机制（四支柱研究支柱三）的一个子目标。

> 置信度：0.88`,
    l3: `### 上下文组装的深层问题

**问题 1：固定配比 vs 动态分配**

当前的四区配比是固定的。但不同场景对分区的需求差异极大：

- **事实查询**（「张三的邮箱是什么」）：recall 区最需要扩大
- **多轮推理**（「我们来讨论一下项目方案」）：history 区主导
- **指令执行**（「帮我写一封邮件」）：system 区 + 当前输入更关键，recall 和 history 只需要少量

一个固定的分区配比不可能同时最优这三个场景。动态分配需要两个前置条件：
1. 在组装前知道当前查询的意图类型（Planner 的 5 类分类可以做到）
2. 每个分区有独立的 token 上限（当前是整体计算后触发溢出，不是分区独立控制）

**问题 2：分级响应与预算联动**

分级响应的思想（q4.5 已详细讨论）对上下文组装有直接影响：如果当前查询走「本地小模型」管线，窗口大小可能只有 8k，预算压力远大于走「云端大模型」管线的 128k 窗口。但当前 \`_build_system_prompt()\` 不感知模型身份——它对 DeepSeek 和本地小模型用的是一套组装逻辑。

这会导致：本地小模型上可能因为预算不足而丢失关键信息，但 DeepSeek 上同样的预算却绰绰有余。一个更合理的做法是让组装逻辑接收一个 \`model_config\` 参数，包含当前模型的窗口大小和推荐的四区配比。

**问题 3：跨轮组装的记忆残留**

每一轮对话，上下文都是重头组装。但有些信息应该跨轮次保留，即使它们不在当前 recel 的 Top-K 中：

- **未完成的目标**：用户正在做的事情，即使当前轮次没提到也不能丢
- **敏感约定**：用户说过的「不要用繁体字」「提到项目 A 时用代称」，这些约束应该在多轮中保持
- **对话背景**：前几轮建立的共同语境，丢掉会导致模型「失忆」

GlassCortex 的 \`SessionState\` 目前通过 \`messages\` 列表保留完整对话历史，但进入上下文窗口的只是最近一批（由 \`max_history\` 控制）。跨轮传递的「关键记忆」没有独立于对话历史的机制来管理——这是远期记忆固化（四支柱 1.4）的目标之一。

**行业对比**

| 平台 | 组装策略 | 排列方式 | 预算分配 | 动态调整 |
|:----:|:--------:|:--------:|:--------:|:--------:|
| OpenAI Chat API | 滑动窗口 + system 保护 | System-First | 固定（max_tokens） | 无 |
| Anthropic Claude API | 长上下文窗口优先 | 用户输入优先 | max_tokens 分离 | 支持 system 独立 |
| LangChain | Memory 模块链式组装 | 开发者自定义 | 无内置预算 | 通过 callback 自定义 |
| MemGPT/Letta | 三层存储 → 热层上下文 | 系统维护 | 分层预算 | 动态迁移 |
| **GlassCortex** | **\_build\_system\_prompt() 固定管线** | **System-First 硬编码** | **固定配比 + 溢出触发** | **未实现** |

> 置信度：0.85

---

\`\`\`mermaid
%% title: 图：上下文组装决策流程
graph TD
    BUDGET["① 计算可用预算<br/>window_size - max_tokens"] --> ZONES["② 分配四区预算<br/>System / Recall / History / Tools"]
    ZONES --> FILTER["③ 各分区内容筛选<br/>按优先级从高到低"]
    FILTER --> OVERFLOW_CHECK{"总预算 <br/>是否超出？"}

    OVERFLOW_CHECK -->|未超出| ORDER["④ 最终排列<br/>System-First 排列"]
    OVERFLOW_CHECK -->|超出| STRATEGY{"溢出策略"}

    STRATEGY -->|Truncate| TRUNCATE["丢弃最早/最低优先级的"]
    STRATEGY -->|Prioritize| PRIORITIZE["按相关度保留，丢弃低分的"]
    STRATEGY -->|Summarize| SUMMARIZE["低分内容压缩为摘要"]

    TRUNCATE --> ORDER
    PRIORITIZE --> ORDER
    SUMMARIZE --> ORDER

    ORDER --> ASSEMBLED["已组装上下文<br/>→ 发送给 LLM"]

    style BUDGET fill:#e8f5e9
    style ZONES fill:#e3f2fd
    style FILTER fill:#fff3e0
    style ORDER fill:#f3e5f5
    style ASSEMBLED fill:#e8f5e9
\`\`\``,
  },
  {
    id: "q1.9",
    question: '多轮对话中的上下文携带策略：跨轮次哪些信息保留、哪些丢弃？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P1",
    confidence: { l0: 0.95, l1: 0.92, l2: 0.90, l3: 0.88 },
    overallConfidence: 0.88,
    l0: '跨轮次携带的信息分三层：常驻层（系统指令、用户约束——每轮都在）、会话层（当前目标、未完成任务——跨轮追踪）、临时层（上一句话的精确措辞——用完即弃）。核心原则是「能复用不重算，能丢弃不携带」。',
    l1: `想象你在不同会议上跟同一个人聊天：

- **在办公室**（第一轮）：你们聊了项目进度，他提到「下周汇报要用英文写」
- **在走廊**（第二轮）：你问项目细节，他不重复「要英文」，直接说数据——因为他默认你还记得
- **在下班路上**（第三轮）：你再提项目，他直接说「结论发我就行」

人的交流天然会「携带上下文」——不需要每轮重新自我介绍，不需要重复之前的约定。但 AI 没有这个直觉。每轮对话，AI 看到的「上下文」都需要系统明确告诉它：哪些是新的，哪些是旧的，哪些虽然旧但还是重要的。

这就是**上下文携带策略**（Context Carryover Strategy）要解决的问题。

### 三层信息，三种策略

可以把跨轮次信息分为三个层级，每个层级的携带方式不同：

| 层级 | 内容 | 携带策略 | 工程实现方式 | 典型 token 成本（每轮） |
|:----:|:----|:--------:|:-----------:|:---------------------:|
| 🏛️ **常驻层** | 系统指令、角色设定、输出格式要求 | **每轮注入** | 随 system prompt 固定携带 | ~500-2000 token（可缓存） |
| 📋 **会话层** | 当前用户目标、未完成任务、敏感约定 | **跨轮追踪** | SessionState 维护，按需注入 | ~100-500 token |
| 🗑️ **临时层** | 上一句话的精确措辞、刚用过的中间结果 | **用完即弃** | 滑动窗口自动丢弃最早的消息 | 0 token（丢弃即释放） |

**常驻层**是最稳定的——系统指令每一轮都原样注入，不需要重新从记忆中召回。这就是为什么系统提示词可以缓存（API 层的 prompt caching 功能对常驻层最有效）。

**会话层**是最微妙的——它介于「永久保留」和「临时丢弃」之间。用户在第二轮说的「不要用繁体字」应该延续到第三轮，但用户第三轮说了「把刚才那段总结一下」，第二轮那条约束不会在显式消息中出现。GlassCortex 的 \`SessionState\` 通过 \`messages\` 数组保留完整历史，但进入上下文窗口的只是最近 N 条——关键约束需要通过独立的「持久化偏好」机制来维护（参考记忆固化引擎 ConsolidationCore（Phase 56）的跨轮偏好维护机制）。

**临时层**最有意思——它占用了大部分 token，但价值最低。第一轮你问「张三的邮箱是什么」，第二轮你问「帮我发邮件给他」——「张三的邮箱是什么」这句话在第二轮已经没有任何信息量，但它还在对话历史里占了 10 个 token。滑动窗口的首要作用就是把这些「信息衰减为零」的消息清出上下文。

### 携带 vs 重算

一个反直觉的观察：**有些信息不携带反而更好**。

每轮对话开始前，系统会重新执行记忆召回——根据当前查询从 FAISS 索引中检索最相关的事实。这意味着：

1. 即使不携带上一轮的所有信息，只要当前查询足够精确，相关记忆也能被重新召回
2. 重算（recompute）的代价是一次向量检索（~10ms），远比在上下文中携带大量低价值 token（每轮多花几十到几百 token）便宜
3. 但重算有丢失风险——如果召回的 top-K 里没有上一轮的关键信息，模型就会「失忆」

所以在工程实践中，**常驻层 + 会话层靠携带，临时层靠重算** 是最经济的分配方式。

> 置信度：0.92`,
    l2: `### GlassCortex 的跨轮携带机制

当前实现中，跨轮信息携带集中在 \`src/chat/engine.py\` 的 \`_build_system_prompt()\` 方法中。每一轮对话，该方法都从零开始组装上下文：

\`\`\`python
# src/chat/engine.py:76 — _build_system_prompt 实际实现（简化）
def _build_system_prompt(self, recalled, context_window_size=4096, ...):
    base_header = "你是一个有记忆的 AI 助手。"
    closing = "请参考这些记忆和事实与用户自然地交流..."

    # 溢出检测：基于召回记忆计算是否超出窗口
    result = simulate_overflow(recalled=recalled, window_size=context_window_size, ...)
    self._last_overflow = result

    if not recalled:
        prompt = f"{base_header}你正在和一个真实用户对话。..."
        return prompt, context_meta_dict

    # 按记忆类型分组：episode（对话片段）+ fact（事实）+ summary（摘要）
    lines = [base_header, ""]
    kept_episodes = [it for it in result.kept_items if it["kind"] == "episode"]
    kept_facts = [it for it in result.kept_items if it["kind"] == "fact"]
    kept_summaries = [it for it in result.kept_items if it["kind"] == "summary"]

    if kept_episodes:
        lines.append("## 对话记忆")
        lines.extend(str(it["line"]) for it in kept_episodes)
    if kept_facts:
        lines.append("## 已知事实")
        lines.extend(str(it["line"]) for it in kept_facts)
    if kept_summaries:
        lines.extend(str(it["line"]) for it in kept_summaries)
    lines.append("")
    lines.append(closing)
    return "\\n".join(lines), context_meta_dict
\`\`\`

这暴露了一个关键问题：**\`_build_system_prompt()\` 只处理召回记忆，不处理对话历史**。对话历史由 \`api/routers/chat.py\` 的路由层管理（每轮对话从 SessionManager 中读取历史消息列表拼入 API 调用），而 \`_build_system_prompt()\` 对历史的存在一无所知。

这意味着「会话层信息」的维护在两个模块之间都没有落脚点：
- 引擎层的 \`_build_system_prompt()\` 不感知历史——它的入参只有 \`recalled\` 列表，不含对话消息
- API 层的 \`api/routers/chat.py\` 是无状态的——每次请求只传单条 \`user_input\`，不管理跨轮历史
- 用户在对话中说的「用英文回答」这类偏好约束，既不在 system prompt 中也无法被引擎感知——完全依赖前端或外部框架维护

### 会话层的工程缺口

GlassCortex 的 \`ContextPartitions\`（\`src/context/partition.py\`）把上下文分为四区：system / recalled / history / tools。但「用户偏好」和「当前目标」这类会话层信息，没有一个独立分区来存放——它们要么挤在 system 区（每轮硬编码），要么混在 history 区（被滑动窗口截断）。

一个理论上的解决方案是增加第五个分区 \`preferences\`：

\`\`\`python
@dataclass
class ContextPartitionsV2:
    zones: list[ZonePartition]  # system / recalled / history / preferences / tools
    preferences: list[str] = []  # 用户的跨轮约束（"用英文""不要用繁体"）
    # — preferences 区永远在溢出策略中优先级第二（仅次于 system）
    # — 由会话管理模块维护，不清除、不老化
\`\`\`

但 GlassCortex 目前没有实现这个机制——这是一个已知的工程缺口，标注在远期上下文治理规划中。

### 跨轮携带的 Token 成本

以一个典型的 QA 场景为例：

| 信息类型 | 每轮 token 数 | 携带方式 | 第 1 轮累计 | 第 10 轮累计 | 第 50 轮累计 |
|:--------:|:------------:|:--------:|:---------:|:----------:|:----------:|
| 系统指令 | 800 | 常驻（可缓存） | 800 | 800 | 800 |
| 当前输入 | 50 | 新注入 | 50 | 50 | 50 |
| 对话历史 | 150/条 | 滑动窗口（上限 20 条） | 150 | 2,000（20 条） | 2,000（截断） |
| 用户偏好 | ~0（无独立机制） | — | 0 | 0 | 0 |

关键数字：**20 条历史消息约 2,000 token**——听起来不多，但注意这已经占 64k 窗口的 \~3%。在长对话中（50+ 轮），如果不做截断，历史区可以膨胀到 7,500+ token（>10%）。滑动窗口的截断阈值直接决定了这个数字的下限。

> 置信度：0.90`,
    l3: `### 跨轮携带的三个深层问题

**问题 1：携带的「衰减曲线」**

信息在多轮对话中的价值不是均匀衰减的。用户的精确措辞（临时层）在第一轮后信息量就趋近于零，但用户的「当前目标」可能在 5 轮后仍然有效。当前的「一刀切」滑动窗口（保留最近 N 条消息）对所有消息一视同仁——但不同类型信息的衰减速度差别很大。

一种更精细的做法是按衰减速度分层：

| 信息类型 | 半衰期（轮次） | 工程含义 |
|:--------:|:------------:|:--------|
| 精确措辞 | 1-2 轮 | 仅保留最近几条，优先丢弃 |
| 对话上下文 | 3-5 轮 | 超过 5 轮摘要为 1 句话 |
| 用户偏好约束 | 整场会话 | 独立存储，永不截断 |
| 未完成目标 | 10+ 轮 | 每次会话结束时检查是否已达成 |
| 长期画像事实 | 跨会话 | 由记忆系统管理，不在携带范围内 |

**问题 2：「不该忘记的」vs「不重要但新鲜的」**

这是滑动窗口的核心矛盾：新消息一定是最近的消息（在窗口末尾），但它不一定比窗口中间的消息重要。例如：用户在第 10 轮发了「谢谢」，这条消息占了窗口位置，但第 5 轮说的「我下周要出差」更有价值——前者是新鲜但无用，后者是稍旧但关键。

当前所有 LLM 应用的滑动窗口方案都面临这个问题——没有一个通用的解决方案，因为「重要」本身就是上下文相关的。

**问题 3：携带链的断裂——跨会话**

当前所有携带策略都只在单次会话内生效。如果用户关闭页面、第二天回来开启新会话，之前的「会话层」信息全部丢失——模型不认识你，不记得你昨天说到哪了。

这与记忆系统相关（q1.1 / q2.1 事实抽取）：长跨度的信息依赖需要通过固化到记忆中来跨越会话边界，不能靠上下文携带。但「固化到记忆」的前提是判断哪些会话层信息值得固化——这本身是一个待解决的研究问题。

### 携带策略树总结

\`\`\`mermaid
%% title: 图：跨轮携带策略决策树
graph TD
    START["🎯 跨轮信息携带决策"] --> L1{"属于哪一层？"}

    L1 -->|"🏛️ 常驻层<br/>系统指令、角色、格式"| FIXED["每轮固定注入<br/>（可 API 缓存）"]
    L1 -->|"📋 会话层<br/>当前目标、偏好、约束"| CARRY{"是否跨越到<br/>下一轮？"}
    CARRY -->|"是<br/>（用户偏好/未完成目标）"| PERSIST["独立持久化<br/>→ 独立分区维护"]
    CARRY -->|"否<br/>（用完即过时）"| DISCARD["随历史<br/>滑动窗口丢弃"]
    L1 -->|"🗑️ 临时层<br/>精确措辞、中间结果"| EPHEMERAL["优先丢弃<br/>释放窗口空间"]

    PERSIST --> CHECK{"跨会话<br/>是否有价值？"}
    CHECK -->|"有"| MEMORY["→ 固化到长期记忆<br/>（FactExtractor 抽取）"]
    CHECK -->|"无"| SESSION_ONLY["保留在会话内<br/>下次会话不携带"]

    FIXED --> RESULT["最终上下文窗口"]
    DISCARD --> RESULT
    EPHEMERAL --> RESULT
    SESSION_ONLY --> RESULT
    MEMORY --> RESULT

    style START fill:#4f46e5,stroke:#4338ca,color:#fff
    style FIXED fill:#3b82f6,stroke:#2563eb,color:#fff
    style PERSIST fill:#34d399,stroke:#059669,color:#111
    style DISCARD fill:#f59e0b,stroke:#d97706,color:#111
    style EPHEMERAL fill:#f59e0b,stroke:#d97706,color:#111
    style MEMORY fill:#8b5cf6,stroke:#7c3aed,color:#fff
    style RESULT fill:#e8f5e9
\`\`\`

> 置信度：0.88

---

**相关 Lab**：在 Lab 的「上下文」Tab 中，你可以查看每轮对话的四区 token 分配，直观看到常驻层、会话层和临时层的占比变化。`,
    labLinks: [{ tab: "context", label: "上下文分区分析" }],
  },
  {
    id: "q1.10",
    question: '上下文利用效率度量：怎么判断"窗口用得好不好"？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P2",
    confidence: { l0: 0.95, l1: 0.92, l2: 0.88, l3: 0.85 },
    overallConfidence: 0.85,
    l0: '就像衡量行李箱的利用率——不是看装了多少，而是看装的东西在旅途中发挥了多大价值。核心指标有三个：空间利用率（窗口填了多少）、信息密度（每 token 携带多少有效信息）、信号噪声比（有用内容 vs 冗余内容的比例）。',
    l1: `你看一个行李箱的利用率，不会只看它装满了没有——还要看里面装的是度假用的沙滩裤还是 10 公斤的哑铃。装满哑铃的箱子「利用率」很高，但你不需要。

上下文窗口也一样。**单纯的「窗口填充率」是误导性指标**——你可以把窗口填到 100%，但里面全是重复的系统指令、过时的对话历史、无意义的寒暄。用得好不好，要看三个维度。

### 维度一：空间利用率（Space Utilization）

这是最直观的指标：实际使用的 token 数 ÷ 可用 token 数。

$$
利用率 = \\frac{total\\_tokens}{window\\_size - max\\_tokens}
$$

- 利用率过低（<30%）：窗口没用好，可以注入更多相关记忆或保留更长历史
- 利用率过高（>90%）：输出可能被截断或溢出策略在频繁工作
- 健康区间通常为 50-80%

但光看这个不够——低利用率但信息密度高，比高利用率但全是噪声要好。

### 维度二：信息密度（Information Density）

信息密度衡量每 token 携带了多少「有效信息」。定义取决于场景：

- **事实密度**：每 token 携带了多少个可验证的事实。例如「张三的邮箱是 zhang@example.com，电话是 138xxxx」在 15 个 token 里包含了两个精确事实——密度很高。而「今天天气不错，你觉得呢」在 10 个 token 里几乎没有可提取的事实。
- **指令密度**：每 token 携带了多少条明确的指令约束。例如「用中文回答，不超过 200 字，输出 JSON 格式」在 10 个 token 里有 3 条约束——密度高。而「请友善地回复」在 4 个 token 里只有 1 条软约束。
- **上下文密度**：每 token 对当前查询的相关度。如果 80% 的 token 与当前查询的语义相似度 > 0.6（以 embedding 余弦距离衡量），密度高；如果大部分是历史寒暄，密度低。

### 维度三：信号噪声比（Signal-to-Noise Ratio）

从通信工程借来的概念。在上下文窗口里：

- **信号**（Signal）：对模型正确回答当前查询有帮助的 token
- **噪声**（Noise）：占用窗口但不帮助回答的 token（包括过时信息、重复约束、无关寒暄）
- **SNR** = signal_tokens / total_tokens

高 SNR 意味着：窗口里几乎没有废话，每一条内容都是因为「对回答当前问题有用」才放进去的。

低 SNR 的典型症状——

- **重复性噪声**：系统指令在每轮完全一样，但模型看过 N 遍后信息量趋近于零——虽然 token 还在，但第二遍以后的「信号」大幅衰减
- **时效性噪声**：用户 10 轮前说的「今天帮我查一下北京的天气」在 20 轮后已经没有信息量，但还在对话历史里
- **寒暄噪声**：「好的」「谢谢」「明白了」这类确认性消息几乎不携带事实，但占满了窗口的中间位置

GlassCortex 的 \`ContextPartitions\` 四区划分（system/recalled/history/tools）本质上是帮你在 SNR 上做结构化——把不同类型的内容分开，方便你判断哪个区的噪声最高。

### 三个维度怎么用？

不看单一指标，看组合：

| 空间利用率 | 信息密度 | SNR | 诊断 |
|:---------:|:--------:|:---:|:----|
| 高 | 高 | 高 | 🟢 理想状态——窗口高效运转 |
| 高 | 低 | 低 | 🔴 窗口满了但全是噪声——最差的利用率 |
| 低 | 高 | - | 🟡 空间有富余，说明召回或历史可以多留 |
| - | 低 | 低 | 🔴 不管窗口利用率如何，内容质量需要提升 |

> 置信度：0.92`,
    l2: `### GlassCortex 中的效率度量现状

当前 GlassCortex 的上下文效率度量和可视化集中在三个位置：

**1. 分区 Token 计量（\`src/context/partition.py\`）**

\`ContextPartitions\` 提供了四区的 token 占用统计：

\`\`\`python
@dataclass
class ContextPartitions:
    zones: list[ZonePartition]        # system / recalled / history / tools
    total_tokens: int                  # 总占用
    window_size: int                   # 窗口大小
    usage_pct: float                   # 空间利用率 %
    overflow_occurred: bool            # 是否触发溢出
    overflow_details: str              # 溢出详情
\`\`\`

其中 \`usage_pct\` 就是上文提到的**空间利用率**——当前占用 ÷ 窗口大小。

每一轮对话完成后，ChatEngine 返回 \`context_meta\` 包含这些数据，前端 Lab 页的「上下文」Tab 通过 \`ContextWindowPanel\` 组件渲染成四色分区条。

**2. 溢出历史记录（\`src/context/overflow_sim.py\`）**

\`OverflowSimResult\` 记录了每次溢出策略执行后的详情：

\`\`\`python
@dataclass
class OverflowSimResult:
    strategy: str               # truncate / prioritize / summarize
    window_size: int
    base_tokens: int
    user_tokens: int
    memories_before: int        # 优化前记忆条目数
    memories_token_before: int  # 优化前记忆 token 数
    memories_after: int         # 优化后记忆条目数
    memories_token_after: int   # 优化后记忆 token 数
    dropped_count: int          # 丢弃的记忆条目数
    dropped_items: list[str]    # 丢弃记忆的内容预览
    kept_items: list[...]       # 保留的记忆
    wasted_tokens: int          # 策略认为浪费的 token 数
\`\`\`

其中 \`dropped_items\` 和 \`wasted_tokens\` 直接对应**信号噪声比**——被判定为「低信号」而丢弃的内容。Lab 的「上下文策略对比」功能通过 \`compare_strategies()\` 函数让你在沙箱中模拟不同策略下的 \`memories_token_after\` 差异。

**3. Token 总账（\`src/token_ledger.py\`）**

\`TokenLedger\` 的 \`TokenUsage\` 维护了每次 LLM 调用的 token 消耗记录：

\`\`\`python
@dataclass
class TokenUsage:
    call_point: str         # generate / recall / compress / classify
    prompt_tokens: int      # 输入 token 数
    completion_tokens: int  # 输出 token 数
    timestamp: float        # 调用时间戳

    @property
    def total_tokens(self) -> int:
        return self.prompt_tokens + self.completion_tokens
\`\`\`

通过 \`TokenUsage\` 可以推算**信息密度**的近似值——如果某步的输入 token 很多但输出很少（低 completion/prompt 比），说明要么输入信息密度低（LLM 花了很多 token 看冗余信息才回答），要么问题本身很简单。

### 现有度量的缺口

| 指标 | 当前支持 | 问题 |
|:----:|:-------:|:----|
| 空间利用率 | ✅ \`usage_pct\` | 已有，但前端未作为独立卡片展示 |
| 信号噪声比 | 🟡 间接 | 溢出策略丢弃的信息可视为噪声，但无独立计量 |
| 信息密度 | ❌ 未实现 | 没有 embedding 层面的「有效信息」量化 |
| SNR 变化趋势 | ❌ 未实现 | 跨轮次的 SNR 对比不可用 |
| 输出/输入比 | 🟡 可通过 TokenLedger 推导 | 没有专门的「效率分数」聚合 |

> 置信度：0.88`,
    l3: `### 效率度量的深层问题

**问题 1：信息密度的定义困境**

「信息密度」听起来直观，但落地时几乎无法定义：一条消息的信息量是由「它帮助模型回答了什么问题」决定的——这在生成之前无法精确计算。

学术上，信息内容可以用**自信息量**（Self-information）来衡量：$I(x) = -\\log P(x)$，一个 token 的预测概率越低，它携带的「信息量」越大。但问题是：

1. LLM 的 token 概率分布是私有的——你拿不到每个 token 的 log P
2. 即使拿到了，一条「天气预报：晴」和一个「请注意」的 self-information 很难直接比较
3. Self-information 衡量的是「意外度」，不衡量「对当前任务的有用度」

没有实用的信息密度度量，所有效率指标都是近似的。

**问题 2：SNR 的「信号」定义是任务依赖的**

同样的系统指令，在「写邮件」场景下是信号（定义了格式约束），在「查天气」场景下是噪声（格式约束不相关）。但同一个上下文窗口里可能同时包含多个任务——用户可能在问完天气后紧接着说「帮我把结果写成邮件发给张三」。

这意味着：信号和噪声不是内容本身的属性，而是**内容与当前查询的关系属性**。一个 token 在这一轮是信号，在下一轮可能变成噪声——但你不可能每轮重新计算所有 token 的 SNR。

**问题 3：效率 ≠ 质量**

高利用率、高信息密度、高 SNR 不一定带来更好的回答质量。一个极端例子：把 50 条关键事实塞满窗口（信息密度极高），但模型因为 lost-in-the-middle 效应忽略了中间 30 条，最终回答质量反而不如只放 10 条关键事实 + 40 条辅助上下文的方案。

这说明：**效率度量必须配合任务质量度量一起使用**——只看窗口利用率就像只看发动机转速不看车速。

### 效率度量的理想结构

\`\`\`mermaid
%% title: 图：上下文效率度量框架
graph LR
    UTIL["📊 空间利用率<br/>used / available"] --> DASH["效率仪表盘"]
    DENSITY["📦 信息密度<br/>事实数 / token"] --> DASH
    SNR["📡 信号噪声比<br/>signal / total"] --> DASH
    CACHE["💾 缓存命中率<br/>cached / total"] --> DASH
    OVERFLOW["🚮 溢出代价<br/>saved / lost tokens"] --> DASH

    DASH --> WARN{"效率阈值检查"}

    WARN -->|利用率 < 30%| LOW["🟡 窗口未充分利用<br/>→ 增加召回深度或历史保留"]
    WARN -->|利用率 > 90%| HIGH["🟠 窗口接近满<br/>→ 检查溢出策略是否频繁触发"]
    WARN -->|SNR < 0.4| LOW_SNR["🔴 噪声过高<br/>→ 检查历史截断和召回质量"]
    WARN -->|缓存率 > 50%| GOOD_CACHE["🟢 缓存效果良好<br/>常驻层 token 节省显著"]

    style UTIL fill:#3b82f6,stroke:#2563eb,color:#fff
    style DENSITY fill:#8b5cf6,stroke:#7c3aed,color:#fff
    style SNR fill:#f59e0b,stroke:#d97706,color:#111
    style CACHE fill:#34d399,stroke:#059669,color:#111
    style OVERFLOW fill:#ef4444,stroke:#dc2626,color:#fff
    style DASH fill:#4f46e5,stroke:#4338ca,color:#fff
\`\`\`

### 接下来的方向

在 GlassCortex 中，效率度量仪表盘（上图中的 mermaid 框架）是规划中的功能，位于四支柱「Token 效率」和「上下文工程」的交叉点。技术上需要的前置条件：

1. **跨轮 Token 轨迹记录**（远期上下文治理规划）——只有知道每一轮 token 的变化，才能计算趋势
2. **信号噪声的自动分类**——需要定义一个启发式规则来判断「什么算信号」
3. **效率仪表盘前端组件**（新 Lab Tab 或现有面板扩展）

当前可以通过 Lab 页的「上下文」Tab 手动对比各轮的 \`usage_pct\` 和溢出统计来获得效率的粗略画像——不自动化，但可人工观察。

> 置信度：0.85

---

**相关 Lab**：在 Lab 的「Token」Tab 中，\`TokenDashboardPanel\` 和 \`CostWaterfallPanel\` 提供了全链路 token 和成本的可视化，可以作为效率评估的辅助数据来源。
`,
    labLinks: [{ tab: "context", label: "上下文使用统计" }],
  },
  {
    id: "q1.11",
    question: '结构化信息 vs 非结构化信息在上下文中的编码效率对比',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P2",
    confidence: { l0: 0.88, l1: 0.82, l2: 0.72, l3: 0.68 },
    overallConfidence: 0.68,
    l0: '用结构化格式（JSON/表格）包装信息就像用集装箱运货——标准化、可压缩、节省空间，但装卸成本更高；用自然语言则是面对面对话——灵活、有温度，但每个字都得自己掏钱。核心取舍在于是要「精确传输数据」还是「让模型深入理解含义」。',
    l1: `想象你用两种完全不同的方式记录同一件事。

**方式一：结构化记录**
\`\`\`json
{
  "name": "张三",
  "age": 28,
  "city": "北京",
  "job": "工程师",
  "skills": ["Python", "数据分析"],
  "project": "GlassCortex"
}
\`\`\`

**方式二：自然语言描述**
> 张三今年 28 岁，是北京的一名软件工程师。他精通 Python 和数据分析，目前参与 GlassCortex 项目。

信息完全相同，但 token 效率差异明显：

| 格式 | Token 数 | 相对效率 | 适合场景 |
|:----:|:--------:|:--------:|---------|
| JSON（精简） | ~15 | 1.7x | 参数传递、枚举值、配置 |
| Markdown 表格 | ~20 | 1.3x | 对照信息、属性列表 |
| 自然语言 | ~26 | 1.0x（基准） | 推理链条、元说明、用户指令 |

### 为什么结构化格式在 token 效率上通常更高？

因为结构化格式利用了**模式复用性**——JSON 的键名和分隔符可以被 tokenizer 高效编码：同一个键名第二次出现时子词拆分结果与第一次完全相同。而自然语言每个词的拼写、上下文修饰语各不相同，无法复用这种「偏旁部首」级别的效率。

但这不意味着结构化格式总是更好。当需要模型**理解**而非**传输**某个信息时，自然语言的语义效率更高——一段 30 token 的比喻解释了一个复杂概念，同样的概念用 JSON 表达可能需要 100 token。

### 什么时候用什么

**结构化优先**：函数调用参数、意图分类标签、配置项、对比表——这些信息精确、格式固定，结构化编码可节省 30-50% token。

**自然语言优先**：推理过程、模糊知识、用户原始输入、元说明——这些信息依赖模型的语义理解，自然语言直接且高效。

**混合编码**（最推荐）：用结构化框架组织信息骨架（标题、分区、元数据标签），用自然语言填充内容血肉。GlassCortex 的 \`_build_system_prompt()\`（src/chat/engine.py:76）就是这种模式——系统指令用自然语言，但召回记忆按 episode/fact/summary 三区结构化组织。`,
    l2: `在 GlassCortex 中，结构化与非结构化的效率差异贯穿多个数据路径。

### 调用层：各分区的格式占比

\`compute_partitions()\`（src/context/partition.py:47）将上下文窗口分为四区，每区天然有不同的格式特性：

| 分区 | 内容格式 | 结构化程度 | 平均 token 占比 |
|:----:|:--------:|:---------:|:--------------:|
| system | 自然语言指令 + 结构化分区头 | 中 | ~20% |
| recalled | 结构化块（episode/fact/summary 标签 + 分隔符） | 高 | ~30% |
| history | 原始对话文本 | 低 | ~40% |
| tools | 函数调用 JSON | 高 | ~10% |

### 计量数据中的编码效率

\`TokenLedger\`（src/token_ledger.py:34）记录每次 LLM 调用的 token 明细。根据实际计量：

- 系统提示词中，结构化分区头（\`## 对话记忆\`、\`## 已知事实\`）仅占约 5-10 token，对总成本的贡献极小
- 召回记忆块使用统一模板：每个 episode 被包裹在 \`- [时间戳] 内容\` 的结构中，结构化元信息约占总成本的 2-3%
- 对话历史是纯非结构化文本，占 token 消耗的大头（~40%），但这是无法压缩的——用户怎么说，你就得怎么记

### 结构化的隐藏成本

结构化格式虽节省 token，但增加了解析复杂度。每一层 JSON 嵌套、每一条 schema 约束都在增加模型解析出错的概率。对于 GPT-4 级别的大模型，简单 JSON（2 层以内）几乎零解析负担；但对于小型模型，嵌套 JSON 可能导致信息遗漏。

**工程结论**：结构化编码的 token 效率优势在**高频调用场景**（每次节省 10-50 token，累积数百轮）才显著。在低频场景，直接使用自然语言更简单可靠。

- \`ContextPartitions\` 分区模型：src/context/partition.py:34-80
- \`_build_system_prompt()\` 混合格式组装：src/chat/engine.py:76-150
- \`TokenUsage\` 计量结构：src/token_ledger.py:10-30`,
    l3: `结构化 vs 非结构化编码的选择牵涉几个深层议题。

### 语义密度 vs 符号密度

「信息密度」有两种不同的度量：**符号密度**（每个 token 携带多少信息单元）和**语义密度**（每个 token 携带多少可理解含义）。前者容易测量（数 token），后者是模型理解深度。一个压缩后的 JSON 可能在符号密度上碾压自然语言，但模型从中提取的含义可能不如一段精心撰写的段落。**效率度量不能只看符号层面**。

### 模型的格式偏好

不同模型对结构化输入的处理能力差异显著——GPT-4 对 JSON 的解析能力较强，而小型模型在处理嵌套结构时可能出现偏差。这意味着「编码效率」要看模型理解和回答的综合效果，而非单纯的 token 数。

### 混合编码的前沿

当前最有前景的方向是**结构化骨架 + 自然语言填充**：
1. 用结构化框架组织层级（标题、分区、标签）
2. 用自然语言填充内容（保持理解深度）
3. 关键数据双重表达（兼顾效率与可靠性）

在 RAG 系统中这已有实践——文档内容保持自然语言，元信息（来源、日期、分数）用结构化标签附着。

### 质量编码检查清单

\`\`\`mermaid
%% title: 图：信息格式选择流程图
graph TD
    DATA["📄 待编码信息"] --> TYPE{"内容类型？"}

    TYPE -->|"参数/配置/枚举值"| STRUCTURED["结构化优先<br/>JSON / 表格 / key=value"]
    TYPE -->|"叙事/解释/推理"| NL["自然语言优先<br/>完整句子 + 段落"]
    TYPE -->|"事实+说明的混合"| HYBRID["混合编码<br/>结构化骨架 + 文本填充"]

    STRUCTURED --> FREQ{"是否高频调用？"}
    FREQ -->|"是（>100轮/会话）"| COMPACT["精简键名<br/>移除冗余空格<br/>单层嵌套"]
    FREQ -->|"否"| BALANCE["保持可读性<br/>使用 Markdown<br/>备注上下文"]

    NL --> DENSE{"是否需要压缩？"}
    DENSE -->|"是"| EXTRACT["提取核心事实<br/>转为结构化存储"]
    DENSE -->|"否"| KEEP["保留原始文本<br/>模型理解最好"]

    HYBRID --> SEPARATE["结构化在前作为骨架<br/>自然语言在后填充内容"]

    COMPACT --> RESULT["✅ 编码完成"]
    BALANCE --> RESULT
    EXTRACT --> RESULT
    KEEP --> RESULT
    SEPARATE --> RESULT

    style DATA fill:#4f46e5,stroke:#4338ca,color:#fff
    style STRUCTURED fill:#3b82f6,stroke:#2563eb,color:#fff
    style NL fill:#34d399,stroke:#059669,color:#111
    style HYBRID fill:#8b5cf6,stroke:#7c3aed,color:#fff
\`\`\`

**参考资源**：Liu et al. (2023) 「Lost in the Middle」——对位置编码与密度的讨论；InstructGPT 论文——格式指令对任务表现的影响。

> 置信度：0.68

---

**相关 Lab**：在 Lab 的「上下文」Tab 中，\`ContextPartitions\` 面板展示了四区的 token 占比，可以直观对比结构化分区（system/recalled）与非结构分区（history）的效率差异。`,
    labLinks: [{ tab: "context", label: "四区 token 占比分析" }],
  },
  {
    id: "q1.12",
    question: '上下文窗口的"暗物质"——system prompt 膨胀治理',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P1",
    confidence: { l0: 0.95, l1: 0.92, l2: 0.88, l3: 0.85 },
    overallConfidence: 0.85,
    labLinks: [{ tab: "context", label: "system 区占比分析" }],
    l0: 'System prompt 像宇宙暗物质——你看不见它，但它确确实实占据着上下文窗口的质量，而且还在不断增长。治理的核心是四分法：分层缓存常驻层、按需编译规则层、合并压缩冗余指令、监控膨胀趋势曲线。',
    l1: `天文学家发现宇宙中有一种东西：看不见、摸不着，但所有星系的运动都受到它的引力影响——他们称之为「暗物质」。它不发光，但它的质量实实在在地扭曲着时空。

上下文窗口里也有类似的东西：**System Prompt**（系统提示词）。

你每轮对话都在带上它，但它不像对话历史那样显眼——你不会在 Lab 页面的聊天记录里看到它。它像一个不可见的底座，每一轮对话都固定在窗口的最前面。你问了多少问题，它就出现了多少次。随着项目发展、功能增多、约束细化，底座越做越大——从最开始的「你是一个 AI 助手」几个字，膨胀到几百上千 token 的角色设定、规则清单、示例、格式约束。

这就是 System Prompt 的膨胀问题。它不像消息溢出那样触发截断（因为它通常排在所有内容之前），也不像重复信息那样有显式的去重机制——它的膨胀是渐进的、隐蔽的，但每轮对话都在为它付出固定的 token 成本。

### 膨胀的四大来源

**来源一：角色指令叠加**

每个新能力都可能要求在 system prompt 中追加一段描述：

- 最开始：「你是一个 AI 助手」
- 加记忆能力：「你有一个记忆系统，可以召回之前的对话」
- 加工具调用：「你可以调用以下工具……」
- 加人格设定：「你喜欢用类比解释复杂概念」
- 加安全规则：「不要生成有害内容……」

五条叠加下来，角色定义已经从一句话膨胀到一段话。每条单独看都有道理，但合在一起就像给一张名片不断加字——最终从名字变成了简历。

**来源二：规则清单化**

功能开发的常见做法：发现 edge case → 加一条规则到 system prompt。久而久之，规则列表从 3 条变成 10 条、30 条：

> "如果用户提到时间，优先使用最近的记忆"
> "如果用户提供了英文输入，优先用英文回复"
> "不要重复用户已经知道的信息"
> "当不确定时，说出来而不是编造"
> "使用 Markdown 格式输出"
> "在回复中引用来源"

每条规则单独都有存在理由，但放在一起时，它们之间可能重叠、矛盾、或者只是占据了空间而没有实际触发场景。

**来源三：示例堆积**

Few-shot 示例是最容易被「再加一个」的。一个示例能提升准确率，再加一个针对新场景的示例更好——3 个之后，示例区已经占了 system prompt 过半的 token 量。

而且示例的「更新」很少发生——新示例总是追加，旧的无人清理。一年后你可能发现 system prompt 里有 8 个示例，其中 3 个针对的场景已经不再支持。

**来源四：格式约束**

JSON schema、输出格式模板、枚举值定义——这些是 token 消耗大户。一个中等复杂度的 JSON schema 轻松吃掉几百 token，而且 schema 本身的结构化标签（键名、括号、引号）在 token 化后比自然语言占得更多（因为 tokenizer 对重复结构不友好）。

### 隐性成本三角

| 成本维度 | 为什么是隐性的 | 量化示例 |
|:--------:|:-------------:|:---------|
| 🪙 **固定消耗** | 不像历史消息可以被溢出策略压缩，system prompt 每轮完整注入 | 每轮 1500 token × 10 轮/天 × 30 天 = 450k token/月 |
| 💾 **缓存失效** | 超 API 缓存上限后每轮重传整段（如 Anthropic prompt caching 上限 1024 token 片段） | 1500 token 超出缓存上限 → 每轮多付 ~100% 输入 token 费 |
| 🎯 **注意力稀释** | 关键指令淹没在无关文本中，Transformer 注意力分布被均匀化 | 3 条关键规则混在 1500 token 中 → 注意力权重被稀释到 2% |
| 🔄 **维护负担** | 改一条规则需要通读全量，怕改了这个那个出问题 | 2000 token 的 system prompt 中改一句话 → 需要做回归测试 |

> 置信度：0.92`,
    l2: `### 五条工程治理策略

System prompt 膨胀不是不可避免的。以下五条策略按「破坏性」升序排列——越早实施破坏越小、收益越高。

#### 策略一：分层架构（Core + Plugin）

这是最基础也最有效的治理。不把 system prompt 存成一个巨大的字符串，而是拆成「核心层」和「插件层」：

- **Core**：角色定义、最通用规则（输出格式、安全约束）——每轮固定注入，生命周期最长
- **Plugin**：按需加载的指令块（工具定义、特殊场景规则、few-shot 示例）——只有触发条件才注入

GlassCortex 当前的 \`_build_system_prompt()\`（\`src/chat/engine.py:76\`）是硬编码的线性拼接——一个 \`base_header\`、一个 \`closing\`，中间是召回的对话记忆和事实。没有分层结构。每条新能力都会直接追加到 \`base_header\` 或 \`closing\`，日积月累自然膨胀。

一个分层重构的伪代码示意：

\`\`\`python
# 理想的分层架构 — 当前未实现
class SystemPromptManager:
    def __init__(self):
        self.core = "你是一个有记忆的 AI 助手。"  # 常驻，每轮注入
        self.plugins: dict[str, str] = {}         # 按 key 注册，按需追加

    def register_plugin(self, key: str, content: str, trigger_condition: Callable) -> None:
        self.plugins[key] = (content, trigger_condition)

    def build(self, query_context: dict) -> str:
        layers = [self.core]
        for key, (content, condition) in self.plugins.items():
            if condition(query_context):  # 只有满足条件才注入
                layers.append(content)
        return "\\n\\n".join(layers)
\`\`\`

**关键**：不是所有规则每轮都有用。「工具定义」只在用户提到某个工具时有效，「安全规则」只在特定上下文触发——按需注入可以节省 30-60% 的 system prompt token 量。

#### 策略二：利用 Prompt Caching

Anthropic 和 OpenAI 都已经支持 prompt caching——如果你的 system prompt 在连续请求间保持不变，API 会缓存其前缀部分，只对新追加的内容收费。

在分层架构下，\`core\` 层因为每轮相同，天然是缓存的绝佳对象。但如果 system prompt 是一个整体字符串（包含每次变化的内容），缓存的收益大幅下降——因为只要有一个字符变了，整个缓存就失效了。

当前 \`_build_system_prompt()\` 每次调用都会重新组装包含记忆内容在内的完整 prompt——虽然 \`base_header\` 和 \`closing\` 不变，但中间的记忆列表每轮不同，导致整个 prompt 在 API 层无法被有效缓存。

#### 策略三：运行时编译

借鉴编译原理的思想——不存储一个巨大的「字节码」，而是存储一组「源代码片段」，在运行时按需编译为最终 prompt。

\`\`\`python
# 运行时编译示意
SYSTEM_PROMPT_FRAGMENTS = {
    "role": {"text": "你是一个 AI 助手", "condition": "always"},
    "memory_context": {"text": format_memories(memories), "condition": "always"},
    "tools": {"text": format_tools(tools), "condition": tools_available},
    "json_output": {"text": format_schema(schema), "condition": output_type == "json"},
    "code_review": {"text": CODE_REVIEW_RULES, "condition": intent == "code_review"},
}

def compile_system_prompt(intent: str, memories: list, tools: list) -> str:
    fragments = [v["text"] for v in SYSTEM_PROMPT_FRAGMENTS.values() if v["condition"]]
    return "\\n\\n".join(fragments)
\`\`\`

这种做法的好处是：每条规则独立维护、独立版本追踪、独立命中率统计。如果「code_review」规则 100 轮都没被触发过，你就有数据支撑把它移出 core 或直接删除。

#### 策略四：指令去重与合并

两条规则「如果用户提供了英文，优先用英文回复」和「保持回复语言与输入一致」——它们说的是同一件事。但在清单式增长中，两条规则会共存。

人工或半自动的规则合并流程：

1. 提取每条规则的「核心主张」（claim extraction）
2. 对相似主张做语义比对
3. 多条相似规则合并为一条通用规则 + 细化条件
4. 合并记录标注在系统的 \`ContextPartitions\` 中

在 GlassCortex 的 \`src/context/partition.py\` 中，\`ContextPartitions\` 目前只记录了四区的 token 占用，没有精细到「rules 内部哪些是冗余的」——这是一个可扩展方向。

#### 策略五：压缩摘要

对低频规则和长期不用的 few-shot 示例，不是删除，而是压缩为一句摘要：

> 原规则（35 token）："当用户提到多个时间点时，优先使用最晚的时间点作为基准，除非用户明确指定了另一个时间点作为参考。"
> 摘要（12 token）："时间点冲突时：最新优先，除非有明确指定。"

需要原文时，在摘要上追加 \`展开注释\` 标记，模型需要详细内容时再提示展开——类似于 q1.17 讨论的「上下文水合」概念。

### GlassCortex 当前的膨胀画像

当前与 system prompt 相关的代码分散在多个位置：

| 模块 | prompt 用途 | 当前大小（估算） | 膨胀风险 |
|:----:|:-----------|:--------------:|:--------:|
| \`src/chat/engine.py\` | 对话 system prompt（head + closing） | ~150 token（纯文本） | 🟡 低——但每次加能力都会扩 |
| \`src/memory/fact.py\` | 事实抽取 prompt | ~200 token（6+ 条抽取规则） | 🟠 中——规则越多抽取越精准？还是越容易被稀释？ |
| \`src/planner/intent.py\` | 意图分类 prompt | ~300 token（5 类定义 + 示例） | 🟠 中——每加一类意图都会追加 |
| \`src/planner/plan.py\` | 任务规划 prompt | ~250 token（规划步骤 + 约束） | 🟡 低——结构相对固定 |

合计约 900+ token 的 system prompt（不含对话中的记忆追加部分）。每轮对话至少为这些 prompt 支付 900 token 的输入成本。

在 64k 窗口中共占比 ~1.4%，不高。但注意：
1. 如果未来再加 5 个功能，可能翻倍到 2000+ token（~3% 窗口）
2. 对于本地小模型（8k 窗口），900 token 已经占掉 ~11% ——压力大很多
3. 对话外的事实抽取和意图分类每次调用都带整段 prompt → 累积成本最高

### 膨胀检测指标

| 指标 | 含义 | 健康阈值 | 告警阈值 |
|:----:|:----|:--------:|:--------:|
| system_pct | system prompt 占窗口比例 | <10% | >20% |
| system_growth_rate | 月均 token 增长率 | <5%/月 | >20%/月 |
| rule_hit_rate | 每条规则的实际触发率 | >30% | <10%（该规则该移出） |
| cache_hit_ratio | prompt caching 命中率 | >80% | <50% |
| attention_entropy | 注意力在 system 区的分布均匀度 | — | 上升趋势（稀释信号） |

当前 GlassCortex 尚没有实现这些指标的自动采集——\`ContextPartitions\` 可以给出 \`system_pct\`，但 \`system_growth_rate\` 需要跨版本数据存储，\`rule_hit_rate\` 需要 Pluggable 架构的支持。

> 置信度：0.88`,
    l3: `### system prompt 膨胀的深层问题

**问题 1：「功能的必要复杂度」陷阱**

每条规则在加入时都是「必要的」——没有人故意加一条没用的规则。但系统的复杂度不是每条规则复杂度的和，而是它们交互复杂度的乘积。

两条规则：「如果用户用英文，就用英文回复」和「优先使用最近记忆中的语言设置」——各自简单，同时存在时，模型在某些场景下可能用中文回复英文问题（因为记忆中有「用户用中文」），或者在英文问题中混入中文术语。

这种复杂度不可通过「再多加一条规则解决」——加规则只会让乘积继续增长。解决方向是规则的正交化：每条规则独立、不重叠、不依赖其他规则解释。这是个架构问题，不是内容问题。

**问题 2：谁为膨胀负责？**

在项目的自然演进中，没有人对 system prompt 的总大小负责。PM 加功能要求加约束，工程师发现 edge case 加规则，安全团队加安全限制——每个人都只对自己加的那部分负责，但没有人看总量。

这个「责任真空」导致了典型的公地悲剧：每加一条规则对个体贡献者的收益是 1（解决问题），而成本被分担到整个系统的 token 消耗上（微乎其微的单轮增量，乘以运行次数后的总成本很大）。要打破这个模式，需要把 system prompt 膨胀纳入工程管理流程——每次变更走 review、标注 token 增量、定期做膨胀审计。

**问题 3：分层与动态性的根本矛盾**

分层架构在工程上是最干净的解法，但有一个理论难题：**你无法提前知道当前查询需要哪些规则**。

如果规则 A（多语言处理）只应在特定场景被触发，你需要在组装 prompt 之前就能判断当前场景——但场景判断本身可能需要 AI 来做，这就成了一个鸡生蛋问题（要判断是否需要规则 A，需要先跑一次 AI 来判断当前意图——那加这层判断就已经多花了一次调用的 token）。

实际的折中方案是离线分类 + 启发式匹配：
- 离线：用历史流量分析每种场景下哪些规则被实际命中
- 在线：用关键词/embedding 相似度做轻量级场景匹配（不调用 LLM）
- 匹配结果决定哪些插件层需要注入

这个方案不需要额外 API 调用，但引入了一个新的工程复杂度：场景匹配器的维护。

**行业对比**

| 平台 | 膨胀治理策略 | 实现层级 | 动态性 |
|:----:|:----------:|:--------:|:------:|
| OpenAI Chat API | system 固定注入，用 max_tokens 隔离 | 客户端管理 | 无 |
| Anthropic Claude API | 独立的 system 参数（不再拼接），支持 prompt caching | API 层 | 无 |
| LangChain | BasePromptTemplate 组合链，支持 fragment 合并 | 框架层 | 开发者在代码中控制 |
| LlamaIndex | SystemPromptBuilder，支持按 query 构造 | 框架层 | 静态但可配置 |
| MemGPT/Letta | 内存分层 + 系统指令按需注入 | 记忆层 | 动态（根据存储内容） |
| Dspy | LLM 生成的 prompt 自动优化 | 编译层 | 自动压缩 |
| **GlassCortex** | **硬编码拼接 \`_build_system_prompt()\`** | **引擎层** | **无动态性** |

GlassCortex 目前处于「简单拼接」阶段——没有分层、没有按需注入、没有膨胀监控。这与项目对 system prompt 的当前需求规模是匹配的（900+ token，占 64k 窗口的 ~1.4%），但如果项目扩展到多模型路由（本地 + 云端混合）、多语言支持、工具调用等场景，膨胀治理将成为刚性需求。

> 置信度：0.85

---

\`\`\`mermaid
%% title: 图：system prompt 膨胀的四大来源与五条治理策略
graph TD
    ROOT["📦 System Prompt 膨胀"] --> SOURCE1["🎭 角色指令叠加<br/>每个新能力追加一段描述"]
    ROOT --> SOURCE2["📋 规则清单化<br/>edge case → 加一条规则"]
    ROOT --> SOURCE3["📝 示例堆积<br/>再加一个 few-shot"]
    ROOT --> SOURCE4["🔧 格式约束<br/>JSON schema / 输出模板"]

    SOURCE1 --> COST1["🪙 固定消耗<br/>每轮完整注入"]
    SOURCE2 --> COST2["🎯 注意力稀释<br/>关键词令淹没在文本中"]
    SOURCE3 --> COST3["💾 缓存失效<br/>超缓存上限后每轮重传"]
    SOURCE4 --> COST4["🔄 维护负担<br/>改一条需通读全量"]

    STRATEGY1["🏛️ 分层架构<br/>Core + Plugin"] --> GOAL["✅ 可控的 system prompt"]
    STRATEGY2["💾 Prompt Caching<br/>缓存常驻层"] --> GOAL
    STRATEGY3["⚙️ 运行时编译<br/>按需组装片段"] --> GOAL
    STRATEGY4["🔗 去重与合并<br/>同类规则归一"] --> GOAL
    STRATEGY5["🗜️ 压缩摘要<br/>低频规则→一句话"] --> GOAL

    style ROOT fill:#4f46e5,stroke:#4338ca,color:#fff
    style SOURCE1 fill:#fca5a5,stroke:#ef4444,color:#7f1d1d
    style SOURCE2 fill:#fca5a5,stroke:#ef4444,color:#7f1d1d
    style SOURCE3 fill:#fca5a5,stroke:#ef4444,color:#7f1d1d
    style SOURCE4 fill:#fca5a5,stroke:#ef4444,color:#7f1d1d
    style STRATEGY1 fill:#34d399,stroke:#059669,color:#111
    style STRATEGY2 fill:#34d399,stroke:#059669,color:#111
    style STRATEGY3 fill:#34d399,stroke:#059669,color:#111
    style STRATEGY4 fill:#34d399,stroke:#059669,color:#111
    style STRATEGY5 fill:#34d399,stroke:#059669,color:#111
    style GOAL fill:#4f46e5,stroke:#4338ca,color:#fff
\`\`\``,
  },
  {
    id: "q1.13",
    question: '上下文"迷失中间"问题：LLM 对窗口中间位置的注意力天然低于开头和结尾',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P2",
    confidence: { l0: 0.92, l1: 0.88, l2: 0.75, l3: 0.72 },
    overallConfidence: 0.72,
    l0: 'LLM 对上下文窗口两端的信息注意力显著高于中间——就像人看一份名单时更容易记住第一个和最后一个名字。这意味着重要的上下文应该放在开头或结尾，而非中间。',
    l1: `假如你面前放着十本书，你最容易注意到的是最左边和最右边的那本——它们有物理边界锚定。中间的书需要你额外花精力去定位。LLM 处理上下文时也有类似的偏差：**模型对窗口开头和结尾的内容给予了不成比例的高关注度，而窗口中间的信息容易被「忽略」**。

Google 和斯坦福的联合研究团队在 2023 年发表了这一现象的正式研究（Liu et al.，标题为 "Lost in the Middle: How Language Models Use Long Contexts"），得出了几个关键发现：

### 发现的规律

1. **U 形注意力曲线**——模型对开头位置的文档回答准确率最高（~80%），末尾位置次高（~70%），中间位置（位置 5-15）的准确率降至 ~50%。
2. **信息位置比信息数量更重要**——在窗口中间放 5 份相关文档，不如在开头放 2 份。不是内容不够好，是模型没有「看到」它们。
3. **不同任务受影响程度不同**——多文档问答受影响最大，单文档摘要受影响较小。

### 为什么会这样？

这涉及 LLM 的注意力机制：

- **开头优势**：开头的 token 充当了「上下文锚点」——模型从这些 token 开始建立对信息的理解框架，后续内容在这个框架内解释
- **末尾优势**：末尾 token 距离输出位置最近，梯度传播路径最短，反向传播时容易被更强烈地更新
- **中间劣势**：中间的 token 既没有锚定效应，又没有近因优势，加上 self-attention 的 softmax 会在长序列中「稀释」中间位置的注意力权重

### 实际影响

这对上下文组装策略有直接影响：

- 放在开头的内容最「贵」但也最「有效」——放最重要的约束和指令
- 放在中间的内容最容易被忽视——放支撑性信息，不做关键依赖
- 放在末尾的内容紧邻输出——放用户最新输入和最直接的指令

> 注意：这个现象会随模型更新而变化。GPT-4-Turbo 和 Claude 3+ 系列通过改进的位置编码在一定程度上缓解了该问题，但 U 形曲线在现有所有模型中仍然存在。`,
    l2: `在 GlassCortex 中，"迷失中间"问题虽然没有专门的缓解机制，但当前的上下文组装设计在客观上起到了部分缓解作用。

### 内容排序的隐式对策

\`_build_system_prompt()\`（src/chat/engine.py:76）的组装顺序天然利用了位置优势：

| 位置 | 内容 | 重要性 | 利用的效应 |
|:----:|------|:------:|:---------:|
| 开头 | 系统提示词（角色 + 规则 + 示例） | ⭐⭐⭐ | 开头锚定 |
| 中前部 | 召回记忆·episode（高相关度） | ⭐⭐⭐ | 脱离中间核心区 |
| 中部 | 召回记忆·fact（已知事实） | ⭐⭐ | 支撑性信息 |
| 中后部 | 召回记忆·summary（压缩摘要） | ⭐ | 可丢弃的中间区 |
| 末尾 | 用户当前输入 | ⭐⭐⭐ | 末尾近因 |

这个排序不是巧合——\`_build_system_prompt()\` 在组装召回记忆时，优先排放 episode（高相关度的对话片段），然后排放 fact（已知事实），最后排放 summary（压缩摘要）。虽然这不一定是为缓解 lost-in-the-middle 而设计，但客观上让最重要（经过 OverflowSim 按相关度排序后保留）的回忆内容落在了更靠近开头的位置。

### 溢出策略的间接缓解

\`simulate_overflow()\` 的三种策略（src/context/overflow_sim.py:108）处理的是"内容太多"的问题，但这些策略也间接影响了位置偏置：

- **Truncate（FIFO 截断）**：丢弃最早的内容，保留最新的。这会让窗口中的内容集中在靠后的位置——末尾注意力高，但开头如果被丢弃了系统指令，整段对话的基础框架就会受损。
- **Prioritize（相关度优先）**：按相关性排序后丢弃低分内容，保留最重要的。这最有希望缓解 lost-in-the-middle——高相关度记忆被前置，低相关度被丢弃而不是堆积在中间。
- **Summarize（压缩摘要）**：保留内容但压缩低分项。这保留了更多信息，但压缩后的摘要本身在中间位置也可能被忽视。

### 现有缓解的不足

当前设计没有专门针对 lost-in-the-middle 的缓解——没有一个机制说"把最重要的 3 条记忆放到开头"或"在中间区域重复关键信息"。这属于远期上下文治理规划的内容。

- 上下文分区排序：src/context/partition.py:34-80
- 系统提示词组装：src/chat/engine.py:76-150
- 溢出策略三方案：src/context/overflow_sim.py:108-290`,
    l3: `「迷失中间」问题牵涉到 LLM 架构的深层次设计取舍。

### 模型差异：不是所有模型都一样

不同模型家族的 lost-in-the-middle 表现差异显著：

- **GPT-4-Turbo（128K）**：显著改善，中间位置准确率从 50% 提升至约 70%，但 U 形曲线仍然存在
- **Claude 3+ 系列（200K）**：在超长上下文测试中表现更均匀，但仍对靠近开头和结尾的内容有偏好
- **Llama 2/3（32K-128K）**：中间位置的退化更明显，尤其在前半段和后半段的信息混杂时

### 为什么越长的窗口问题越严重？

一个反直觉的现象：**上下文窗口越长，「迷失中间」越严重，而不是越轻。** 随着输入长度增加，中间的 token 数增多，注意力分布被进一步稀释。这意味着单纯扩展窗口大小不解决问题——甚至可能放大问题。

### 当前缓解方案

学术和工业界正在探索多种缓解策略：

1. **检索穿插（Retrieval-Interleaving）**：不是在窗口开头一次性放完所有检索结果，而是在推理过程中分段检索、分段插入——让每个检索块都落在开头或末尾的高注意力区
2. **信息重复（Key Information Repetition）**：在开头和结尾同时提及最关键的信息——利用两端的注意力优势做冗余保证
3. **结构化分区增强**：把重要信息封装在开头和结尾的特殊标签中（\`<IMPORTANT>\`...\`</IMPORTANT>\`），引导注意力分配
4. **位置编码优化**：ALiBi（Press et al. 2021）和改进的 RoPE 正在被证明可以平滑长距离注意力衰减

### 开放问题

lost-in-the-middle 在根本上是一个**注意力分配**问题，而注意力机制是 Transformer 架构的基石。解决它可能意味着：

- 要么改变注意力计算方式（稀疏注意力、分段注意力）
- 要么接受这个限制并设计系统架构来适应它（检索穿插、重复策略）
- 要么等待新一代架构（状态空间模型如 Mamba）解决这个根本问题

\`\`\`mermaid
%% title: 图：迷失中间——原因与缓解策略
graph TD
    WINDOW["📐 LLM 上下文窗口"] --> BIAS{"注意力分布偏差"}

    BIAS -->|"开头 🔝"| HIGH1["高注意力<br/>✅ 放系统指令<br/>✅ 最关键的事实"]
    BIAS -->|"中间 ⚠️"| LOW["低注意力<br/>⚠️ 放辅助信息<br/>⚠️ 支撑性上下文"]
    BIAS -->|"末尾 🔝"| HIGH2["高注意力<br/>✅ 放用户输入<br/>✅ 最近的对话"]

    WINDOW --> MITIGATE{"缓解策略"}

    MITIGATE --> ORDER["① 重要性排序<br/>最重要的放两端"]
    MITIGATE --> DEDUP["② 减少中间堆积<br/>避免无关内容"]
    MITIGATE --> REPEAT["③ 关键信息重复<br/>开头+末尾双保险"]
    MITIGATE --> INTERLEAVE["④ 检索穿插<br/>分段检索+分段插入"]
    MITIGATE --> MODEL["⑤ 模型选择<br/>优先位置编码更优的模型"]

    style WINDOW fill:#4f46e5,stroke:#4338ca,color:#fff
    style HIGH1 fill:#ef4444,stroke:#dc2626,color:#fff
    style LOW fill:#f59e0b,stroke:#d97706,color:#111
    style HIGH2 fill:#ef4444,stroke:#dc2626,color:#fff
\`\`\`

**参考论文**：
- Liu et al. (2023) "Lost in the Middle: How Language Models Use Long Contexts"
- Press et al. (2021) "Train Short, Test Long: ALiBi Positional Encoding"

> 置信度：0.72

---

**相关 Lab**：在 Lab 的「上下文」Tab 中，\`ContextPartitions\` 面板展示了四区的内容分布和 token 占比，可以帮助你观察不同位置的内容密度。`,
    labLinks: [{ tab: "context", label: "上下文分区与位置分析" }],
  },
  {
    id: "q1.14",
    question: '上下文格式编码效率：XML/Markdown/JSON/自然语言对比',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P3",
    confidence: { l0: 0.90, l1: 0.85, l2: 0.78, l3: 0.70 },
    overallConfidence: 0.70,
    l0: '上下文用 Markdown 或自然语言编码在 token 效率上最优（≈1 token/字符），JSON 和 XML 因结构标签消耗额外 token（≈1.5-3× 开销），但结构化格式在机器解析精度上更可靠——这是一场"人读效率"与"机读精度"之间的权衡。',
    l1: `想象你在写一张便签给同事。你写「帮我买杯咖啡，冰拿铁，少糖」——自然语言，7 个字，对方秒懂。但如果对方是一台自动售货机，你得写成这样：

\`\`\`json
{"drink": "latte", "temperature": "cold", "sugar": "less"}
\`\`\`

字多了，但机器不会误会。

LLM 的上下文编码也面临同样的选择。你可以用自然语言告诉模型「你是一个有帮助的助手」，也可以用 XML 包装每段记忆、用 Markdown 划出分区、或者用 JSON 描述结构化信息。**不同的编码格式对 token 消耗、解析精度、模型理解质量有直接影响。**

### 四种格式的优劣

**① 自然语言（Natural Language）**
最直接的编码方式——直接写句子，不加结构标签。
- ✅ Token 开销最小：无冗余标记
- ✅ 人类可读性最好：任何读者都懂
- ❌ 信息边界模糊：模型需要自行推断「这段结束、下段开始」
- ❌ 程序化提取困难：没有固定结构供下游代码解析

**② Markdown**
用标题（##）、列表（-）、代码块（\\\`\\\`\\\`）等标记组织内容。
- ✅ 可读性≈自然语言，但结构更清晰
- ✅ Token 开销小：仅添加少量分隔字符（## 或 --- 等）
- ✅ 当前主流编码：Claude System Prompt、GlassCortex 的 \`_build_system_prompt()\` 都采用 Markdown 风格的结构化文本
- ❌ 标准化程度低：不同渲染器解析不一致

**③ JSON**
用键值对和嵌套结构表达信息。
- ✅ 机器解析准确：确定的层级关系，不会模棱两可
- ✅ 结构化程度最高：适合复杂嵌套数据
- ❌ Token 开销大：键名和引号大量重复（"key": "value" 是 13 个字符表达 2 个信息）
- ❌ 人读不友好：嵌套层级一深，肉眼很难追踪

**④ XML**
用自定义标签包裹每段内容。
- ✅ 显式标签边界：\`<memory>\`...\`</memory>\` 明确标识信息范围
- ✅ 自描述性好：标签名本身就是元数据（\`<priority>high</priority>\`）
- ❌ **Token 开销最大**：\`<tag></tag>\` 的闭合标签消耗双倍结构 token
- ❌ 在 LLM 上下文中的实际表现参差不齐：XML 对 GPT 系列有天然优势（预训练语料包含大量 XML），对 Claude 系列则无明显优势

### 实际选择：没有银弹
GlassCortex 当前采用了 **Markdown 风格的自然语言**——\\\`\\\`_build_system_prompt()\\\`\\\` 用 \\\`##\\\` 标题区分「对话记忆」「已知事实」「压缩摘要」三区，用空行分隔段落。这个选择兼顾了 token 效率（无需大量结构标签）和模型理解（分区明确），同时也允许代码在构建时自然地控制内容边界。`,
    l2: `### 四种格式的 Token 成本对比

以下是一个等价信息块用四种格式编码时的 token 消耗对比（基于 DeepSeek / GPT 兼容 tokenizer 估算，4 字符≈1 token 为近似值）：

| 格式 | 编码样例 | 预估 token | 效率 | 适合场景 |
|------|---------|:----------:|:----:|---------|
| 自然语言 | "AI 助手，友善回应" | ~5 | ⭐⭐⭐ | 短提示、简单指令 |
| Markdown | "## 规则\\n- 友善回应" | ~7 | ⭐⭐⭐ | 系统提示、中等长度上下文 |
| JSON | \`{"role":"assistant","tone":"friendly"}\` | ~14 | ⭐⭐ | 结构化检索、API 返回 |
| XML | \`<role>assistant</role><tone>friendly</tone>\` | ~16 | ⭐ | 标签丰富的结构化数据 |

以 GlassCortex 的实际组装逻辑为例：\\\`_build_system_prompt()\\\`（src/chat/engine.py:76）构建出的系统提示词如果在当前 Markdown 基础上改为 JSON/XML 编码，总计多消耗约 40-60% 的 token——对于一个典型加载 10 条记忆、每条约 20 个词的场景，额外开销约 100-300 token。按 4K 窗口计算，这是窗口总量的 2.5%-7.5%。

### GlassCortex 的实际编码结构

当前项目采用 **简易 Markdown 样式**：

\`\`\`python
# src/chat/engine.py:76-150（关键段落）
lines = ["你是一个有记忆的 AI 助手。", ""]
# 三区独立注入
if kept_episodes:
    lines.append("## 对话记忆")
    for item in kept_episodes:
        lines.append(str(item["line"]))
if kept_facts:
    lines.append("## 已知事实")
    for item in kept_facts:
        lines.append(str(item["line"]))
prompt = "\\n".join(lines)
\`\`\`

为什么选这个？三个理由：
1. **Token 效率优先**：不加 XML 标签或 JSON 键，每个字符都是在传达有效信息
2. **分区足够**：\\\`##\\\` 标题对 LLM 来说已经是清晰的分隔符——不需要更重的结构
3. **代码简洁**：直接拼接字符串 vs 构造 JSON/XML 序列化器，维护成本天差地别

对应的分区结构在 \`src/context/partition.py:9-14\` 的 \`_ZONE_DEFS\` 中定义：

\`\`\`python
_ZONE_DEFS = [
    {"key": "system",    "label": "系统提示",   "emoji": "⚙️"},
    {"key": "recalled",  "label": "记忆召回",   "emoji": "🧠"},
    {"key": "history",   "label": "对话历史",   "emoji": "💬"},
    {"key": "tools",     "label": "工具定义",   "emoji": "🛠️"},
]
\`\`\`

### 什么时候该换格式？
- 当你需要给模型传递大量结构化数据（数据库记录、API 返回值）→ JSON 更可靠
- 当一段内容既供模型读又供程序解析 → XML 是最佳中间格式
- 当只关心最高质量理解（不关心 token 成本）→ 自然语言 + Markdown 仍然不差`,
    l3: `### 格式选择的前沿探索

LLM 对编码格式的敏感度远比你想象的高。一些值得关注的方向：

**1. 标签即元数据（Label-as-Metadata）**
OpenAI 的 "Structured Outputs" 实践表明，当信息标签本身就是语义信号时，模型理解质量会提升。比如 \`<urgent>true</urgent>\` 不只是在表达一个布尔值——它还在暗示"请优先处理这条"。这让 XML/JSON 有了超越 token 效率的价值。

**2. 格式与预训练语料的对齐**
不同模型的母语不同：
- **GPT 系列**：预训练语料中 XML 格式的网页数据极多，对 XML 格式的本能理解最好
- **Claude 系列**：产品文档和结构性文本占主导，对 Markdown-style 的分节说明最敏感
- **Llama 系列**：混合了 Wikipedia 和网页数据，对不同格式的敏感度差异更小

这意味着同样一段 XML 编码的上下文，GPT-4 可能天然比 Llama-3 理解得更好——不是模型能力的差异，是训练语料的差异。

**3. 格式混合策略**
前沿实践正在从"选一种格式"走向"混合编码"：
- 外层用 Markdown（人读、框架结构清晰）
- 内层关键信息用 JSON（机器解析准确）
- 边界用 XML 标签包裹（显式区分内容范围）

比如：
\`\`\`
## 用户信息
<profile>{"id": "u001", "tier": "premium"}</profile>

## 当前指令
请根据用户等级提供个性化推荐。
\`\`\`

GlassCortex 的当前实现已经具备了向这个方向演进的基础——只要在构建 prompt 时选择性注入不同的编码格式即可。不需要改架构，改的是组装策略。

**4. 格式无关的注意力层**
一个更激进的思路来自 LongLoRA 和位置编码优化研究：是否可以让模型本身对格式不敏感？如果注意力机制能自动识别"哪些字符是结构标签、哪些是有效内容"，那编码格式就变得不重要了。目前这仍处于理论研究阶段，工业应用尚远。

### 一个实用的决策框架

面对"用哪种格式"的选择时，按这个顺序判断：
1. 这段内容需要**机器精确解析**吗？→ JSON
2. 这段内容只有**模型需要理解**？→ Markdown 或自然语言
3. 这段内容**既要给人看又要给机器读**？→ XML 或混合编码
4. 我在意 token 成本吗？→ 永远选最简格式，不浪费标签字符

> 置信度：0.70

---

**相关 Lab**：在 Lab 的"上下文"Tab 中，\`ContextPartitions\` 面板展示了当前系统提示的分区结构和 token 占比——你可以观察系统区与记忆区分别用了多少 token，评估当前格式的编码效率。`,
    labLinks: [{ tab: "context", label: "上下文分区与编码效率分析" }],
  },
  {
    id: "q1.15",
    question: '指令层级冲突：system prompt/用户偏好/对话历史冲突时的优先级',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P2",
    confidence: { l0: 0.88, l1: 0.82, l2: 0.75, l3: 0.72 },
    overallConfidence: 0.72,
    l0: '指令冲突的优先级是：System Prompt > 用户偏好 > 对话历史。系统提示词定义不可违反的边界，用户当前指令可以在边界内覆盖偏好设置，历史对话提供上下文但不改变已定规则。',
    l1: `想象你在一个游乐园。不同角色给你不同的指引：

- **园区规则（= System Prompt）**：墙上写着「身高不足 1.2 米的游客不能坐过山车」。这是铁律，谁来都不改。
- **你的偏好（= 用户设置）**：你在 APP 上勾选了「优先玩刺激项目」。这影响路线推荐，但不会让售票员放你上过山车——如果身高不够的话。
- **你朋友聊天的建议（= 对话历史）**：朋友说「那个鬼屋超好玩，一定要去」。你采纳了，但游乐园不会因为朋友说了就让你插队。

**LLM 的指令层级也遵循类似的优先级结构。**

### 三层优先级

**① System Prompt（系统提示词）—— 最高优先级**
它是 AI 的"根本大法"，定义：
- 角色与身份：你是谁（助手/客服/代码解释器）
- 不可违反的约束：不准编造引用、不准输出危险代码、必须引用来源
- 核心行为模式：回答格式、信息密度、引用规则

System Prompt 的优先级是硬性的。模型在训练阶段就已经被训练成 "system message 里的指令优先于其他一切"——这不是可配置的偏好，是 API 层的固有设计。

**② 用户偏好与当前指令 —— 中间优先级**
用户可以在 system prompt 的边界内自主选择：
- 对话风格偏好：默认详细还是简洁
- 输出格式偏好：回答附带代码示例还是纯文字
- 当前指令：在某个具体问题上的明确要求

注意：**当前指令和用户偏好之间也存在竞争**——你之前说「以后回答简短点」（偏好），但现在问了一个复杂问题、明确要求「一步一步分析」（当前指令）。当前指令胜出。

**③ 对话历史 —— 最低优先级**
历史对话提供上下文，影响的是"理解"而非"规则"：
- 你说过自己喜欢 Python——后续回答可能侧重 Python 示例
- 你之前纠正过某个回答——模型会在后续中避免类似错误
- 你提到过你在做数据分析——模型会默认使用数据分析师的术语

但这些影响不是强制的。如果用户当前指令和 system prompt 明确要求了不同行为，历史中的"习惯"会被覆盖。

### 冲突怎么处理？
1. **System Prompt 不可覆盖**——用户偏好和历史都不能绕过系统提示词的限制
2. **当前指令 > 历史偏好**——当前轮的明确要求覆盖之前轮次的隐式约定
3. **历史只当参考**——历史对话的"习惯"体现为注意力权重，而不是规则`,
    l2: `### GlassCortex 中的指令层级

在 GlassCortex 中，指令层级通过 API 调用结构和系统提示词组装机制来体现：

**最外层：API 消息层级**
\`\`\`python
# src/chat/engine.py:179-184
response = self.client.chat.completions.create(
    model=...,
    messages=[
        {"role": "system", "content": system_prompt},   # ← System Prompt：最高优先级
        {"role": "user",   "content": user_input},       # ← 用户输入：次高优先级
    ],
)
\`\`\`
系统提示词走 system role 通道，用户输入走 user role 通道。这意味着你在 API 层就锁死了层级关系——模型不会把 system prompt 里的内容当作"建议"，而是当作不可违反的约束。

**中间层：系统提示词内部的层级 \`_build_system_prompt()\`**
系统提示词内部也有自己的优先级结构：

| 位置 | 内容 | 语义优先级 | 说明 |
|:----:|------|:----------:|------|
| 开头 | \`base_header\` + 角色定义 | ⭐⭐⭐ | "你是一个有记忆的 AI 助手"——角色不可变更 |
| 中前 | \`## 对话记忆\`（episodes） | ⭐⭐ | 高相关度对话片段，形成上下文理解框架 |
| 中后 | \`## 已知事实\`（facts） | ⭐⭐ | 已验证的事实 |
| 末尾 | \`## 压缩摘要\`（summaries） | ⭐ | 低相关度信息的浓缩 |
| 外围 | API 调用中的 \`messages\` 顺序 | ⭐⭐⭐ | system 在 user 之前——不可交换的顺序依赖 |

**底层：偏好传递**
当前用户偏好（如 temperature、输出长度、模型选择）通过 \`generate()\` 的参数传入：

\`\`\`python
# src/chat/engine.py:186
temperature=temperature if temperature is not None else settings.llm_temperature,
\`\`\`

\`settings.llm_temperature\` 是全局配置（相当于 system prompt 层的默认值），\`temperature\` 参数是当前调用可以传入的覆盖（相当于用户偏好覆盖）。如果两者冲突，当前覆盖胜出。

### 当前设计的不足之处
1. 没有明确的"用户偏好"配置文件——偏好靠参数透传，没有集中维护
2. 系统提示词内部没有优先级标注——\`_build_system_prompt()\` 用位置暗示优先级，但没有显式的标签约束
3. 没有处理"多轮对话中偏好漂移"的场景——用户在第 1 轮要求详细回答，第 3 轮说算了简单点——当前代码是 stateless 的，不追踪偏好变化

这些不足属于远期上下文治理规划的范围。`,
    l3: `### 指令层级的行业实践

**1. OpenAI "The Instruction Hierarchy"（2024）**

OpenAI 在 2024 年正式提出了指令层级（Instruction Hierarchy）的概念，将其作为模型安全和对齐的核心机制。其定义的优先级从高到低为：

1. **平台规则**（不可更改，由模型提供方硬编码）
2. **开发者消息**（System Prompt——最高优先级的用户控制层）
3. **用户消息**（当前对话中的 user role 消息）
4. **工具输出**（Tool Call 的返回结果）
5. **对话历史**（多轮对话中的 Assistant 消息和中间消息）

这个层级设计的核心洞察是：**越靠近当前轮次、越靠近开发者侧的指令，优先级越高。** 不是因为"是谁说的"，而是因为"注入攻击的常见路径是让历史中的恶意指令覆盖当前规则"。

**2. Claude 的 System Prompt 机制**

Claude 的 system prompt 有特殊的处理方式：
- 强制不可覆盖：Claude 的角色一致性机制确保 system prompt 的核心约束不会被后续 user 消息覆盖
- 冲突检测：当检测到 user 消息与 system prompt 在关键约束上冲突时，Claude 倾向于拒绝而非服从
- 长上下文中的衰减：即使 system prompt 优先级最高，在极长上下文中（200K+），开头 system prompt 的"锚定效应"也会随距离衰减——这就是 lost-in-the-middle 对指令层级的额外影响

**3. 实际工程中的冲突处理模式**

在实际 AI 应用开发中，常见的冲突处理模式有三种：

- **严格层级**（Strict Hierarchy）：系统提示词不可被任何用户输入覆盖。安全性最高的方案，适合客服、法务等场景。
- **软覆盖**（Soft Override）：用户输入可以在某些维度上覆盖系统提示词（如输出风格、详细程度）。适合助手类场景。
- **协商模式**（Negotiation）：当检测到冲突时，模型向用户解释冲突点并请求确认。适合需要灵活性的高级用户场景。

GlassCortex 当前采用**默认系统提示 + 调用参数覆盖**的软覆盖模式，属于中间的折衷方案。

\`\`\`mermaid
%% title: 图：指令层级与冲突处理流程
graph TD
    SP["⚙️ System Prompt<br/>（最高优先级）"] --> CHECK{"指令冲突？"}
    UP["👤 用户偏好<br/>（中间优先级）"] --> CHECK
    CH["💬 对话历史<br/>（最低优先级）"] --> CHECK

    CHECK -->|"无冲突"| EXECUTE["✅ 正常执行<br/>各层指令叠加生效"]
    CHECK -->|"System Prompt 被挑战"| REJECT["⛔ 拒绝<br/>对话历史/用户输入不能覆盖系统规则"]
    CHECK -->|"用户偏好 vs 对话历史"| PREFERENCE["✅ 用户偏好优先<br/>当前指令 > 历史偏好"]
    CHECK -->|"同层内部冲突"| POSITION["📐 按位置优先级<br/>开头 > 末尾 > 中间"]

    style SP fill:#4f46e5,stroke:#4338ca,color:#fff
    style UP fill:#8b5cf6,stroke:#7c3aed,color:#fff
    style CH fill:#a78bfa,stroke:#9333ea,color:#fff
    style REJECT fill:#ef4444,stroke:#dc2626,color:#fff
    style PREFERENCE fill:#f59e0b,stroke:#d97706,color:#111
\`\`\`

### 未解决的问题

1. **偏好漂移**——用户持续改变偏好时，模型该记住最后一次还是回滚到系统默认？目前没有好的工业实践。
2. **层级层数**——两层（system/user）够吗？需要第三层（工具定义、RAG 上下文、外部 API 返回）吗？OpenAI 的五层方案是方向，但会增加系统复杂度。
3. **冲突检测的自动化**——目前冲突检测靠模型自身的对齐训练，缺乏工程层面的自动化冲突检测工具。

> 置信度：0.72

---

**相关 Lab**：在 Lab 的"上下文"Tab 中，\`ContextPartitions\` 面板显示 system/recalled/history/tools 四区占比，可以观察系统提示在当前窗口中的"话语权"——系统区占比越高，指令层级的固定性越强。`,
    labLinks: [{ tab: "context", label: "上下文分区与指令层级分析" }],
  },
  {
    id: "q1.16",
    question: '上下文的时间维度：按时间排 vs 按话题聚类 vs 混合',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P2",
    confidence: { l0: 0.88, l1: 0.82, l2: 0.78, l3: 0.70 },
    overallConfidence: 0.70,
    l0: '上下文组织方式三种主流：按时间排列（流水账式）、按话题聚类（按主题归堆）、混合策略（话题内按时间）。没有绝对最优——取决于任务类型：时间敏感场景（对话历史）按时间，知识密集场景（事实检索）按话题。',
    l1: `想象你在整理一个人的聊天记录：

- **按时间排**就是把所有对话按时间戳一字排开——"周一说的、周二说的、周三说的"，像日记。最自然，最容易追溯上下文，但你得翻很多无关内容才能找到具体话题。
- **按话题聚类**就是把所有聊过的话题分别整理——"关于 Python 的放一摞、关于旅游的放一摞"。找起来快，但你不知道话题之间的时间关系——是先聊了 Python 再去旅游，还是旅游回来后学的 Python？
- **混合策略**是话题袋子里按时间排——每个话题内部维持时间顺序。兼顾了话题查找和时间追溯，但实现更复杂。

这三种方式在 LLM 上下文组织中各有适用场景。

### 按时间排列（Time-Ordered）

直接把内容按时间顺序拼接——新内容追加到尾部。

\`\`\`
[09:00 用户: 帮我查一下 Python 的 dict 性能]
[09:01 AI: Dict 的插入是 O(1)...]
[10:30 用户: 周末想去爬山]
[10:31 AI: 推荐香山，门票 10 元]
[14:00 用户: 继续刚才 Python 的问题]
\`\`\`

- ✅ 实现最简单：append 就行
- ✅ 时间线清晰：知道每件事发生的顺序
- ✅ 长对话上下文保持良好
- ❌ 主题分散时大量无效信息在窗口里
- ❌ 跨话题查找困难：需要从头翻到尾

### 按话题聚类（Topic-Clustered）

将内容按语义或话题分组，同组内容放在一起。

\`\`\`
[Python: Dict O(1), List O(n), 生成器]
[旅游: 香山, 自驾, 酒店推荐]
[闲聊: 天气, 电影, 八卦]
\`\`\`

- ✅ 信息密度高：同一话题内容相邻，模型一次性读完
- ✅ 查询效率好：找某个话题时上下文集中
- ❌ 时间线丢失：不清楚话题间的先后顺序
- ❌ 跨话题引用困难：一个话题影响另一个话题的判断会丢失

### 混合策略（Hybrid）

最常见的是"分话题 + 话题内按时间"的混合策略。即先按话题聚类，每个话题内部再按时间排序。

这在 GlassCortex 的记忆系统中已经隐含了——\`_build_system_prompt()\` 按 episode → fact → summary 的分区就是粗粒度的"按类型聚类"，而每个分区内部按 recall 引擎的排序输出（综合了相关度和时间衰减）。虽然不是完美的话题聚类，但在不引入额外复杂度的情况下实现了部分混合效果。`,
    l2: `### GlassCortex 的实际组织方式

当前项目既不是纯时间排序，也不是纯话题聚类——而是一种**隐式的混合方案**：

**第一层：按类型分区（粗粒度话题聚类）**

\`_build_system_prompt()\` 在 src/chat/engine.py:117-134 中按类型组装：

\`\`\`python
# 三区独立注入，每区按召回引擎的输出顺序
if kept_episodes:
    lines.append("## 对话记忆")
    for item in kept_episodes:
        lines.append(str(item["line"]))   # ← episode 区
if kept_facts:
    lines.append("## 已知事实")
    for item in kept_facts:
        lines.append(str(item["line"]))   # ← fact 区
if kept_summaries:
    lines.append("## 压缩摘要")
    for item in kept_summaries:
        lines.append(str(item["line"]))   # ← summary 区
\`\`\`

**第二层：区内按综合分排序（隐式混合）**

每区内的顺序由 \`RecallEngine.recall()\` 决定（src/memory/recall.py:95）：

\`\`\`python
scored.sort(key=lambda x: x[1], reverse=True)  # 按 composite_score 降序
\`\`\`

这个 \`composite_score\` 是三个因素的乘积：
- **语义相似度**（话题相关度）—— 来自 Embedding 做语义搜索
- **艾宾浩斯强度**（时间衰减）—— 来自遗忘曲线 \`ForgettingEngine.current_strength()\`，距上次召回越久越弱
- **重要性权重**（全局优先级）—— 每条记忆创建时赋予

\`score = similarity × strength × importance\`

所以时间没有作为独立的排序键出现，而是通过 \`strength\` 组件间接影响排序——越新的记忆强度衰减越少，排名就会靠前。这是一个**弱时间偏好**设计：话题优先、时间在内。

**第三层：MMR 多样性重排（话题内的反聚集）**

\`mmr_rerank()\` 在 src/memory/recall.py:140-212 中通过 MMR 算法避免候选集中在单一话题：

\`\`\`python
MMR = λ·rel(c) - (1-λ)·max_sim(c, S)  # λ=1 纯相关，λ=0 纯多样
\`\`\`

这实际上是在同类话题中主动推开相似内容——避免三条关于"Python 性能"的记忆连续出现。

### 各方案的对比

| 维度 | 纯时间排序 | 纯话题聚类 | GlassCortex 当前 |
|------|:-----------:|:----------:|:----------------:|
| 时间线清晰度 | ⭐⭐⭐ | ⭐ | ⭐⭐ |
| 话题集中度 | ⭐ | ⭐⭐⭐ | ⭐⭐ |
| 跨话题效率 | ⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| 实现复杂度 | ⭐ | ⭐⭐⭐ | ⭐⭐ |
| 长对话支持 | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ |
| 主要缺陷 | 无效信息堆积 | 时间线断裂 | 没有显式时间顺序 |

### 可以改进的方向
1. 在 \`_build_system_prompt()\` 中加入显式时间排序参数——让用户切换"时间优先"模式
2. 在 \`RecallEngine\` 中增加按时间跨度的分桶——把最近 1h/24h/7d 的分段
3. MMR 参数动态化：长对话时降低 λ（更偏向多样性），短对话时提高 λ（更偏向相关性）`,
    l3: `### 时间维度组织的前沿探索

**1. 时间感知的 MMR**
标准 MMR 只考虑语义多样性。时间感知 MMR 增加一个时间惩罚项——最近的内容即使话题相似也保留。公式变为：
\`\`\`
Time-MMR = λ·rel(c) - α·(1-λ)·max_sim(c, S) - β·time_gap(c, last)
\`\`\`
其中 \`time_gap\` 是当前候选与最后选中项的时间差，β 控制时间多样性的强度。

**2. 时间分桶（Temporal Bucketing）**
将上下文窗口划分为时间段，每个时间段内独立排序。比如：
- 最近 1 小时：保留全部内容（时间优先）
- 1-24 小时前：按相关度精选（混合）
- >24 小时前：只保留最高相关度的事实（话题优先）

这种方案在 LlamaIndex 和 LangChain 的 "Time-weighted" retrievers 中已有实现。

**3. 递归时间摘要（Recursive Temporal Summarization）**
放弃精确时间线，转而生成时间粒度的摘要：
- 每次会话结尾 → 生成本次摘要
- 多次摘要 → 合并为日摘要
- 多日日摘要 → 合为周摘要

这不只是排序问题——这是"是否值得保留时间精度"的取舍。时间精度是宝贵的，但不是所有记忆都需要它。

**4. 话题转移检测（Topic Shift Detection）**
更智能的方案是用意图分类器检测话题何时切换，然后自动建立话题边界：

\`\`\`python
# src/planner/intent.py — PlannerEngine 的 5 类意图分类
INTENT_CATEGORIES = ("提问", "指令", "探索", "闲聊", "澄清")
\`\`\`

理论上可以利用 \`PlannerEngine\` 的分类结果做话题分割标识——当意图从"指令"切换到"闲聊"时，认为话题边界存在，自动启用新的话题聚类。这个方向还没在 GlassCortex 中实现，但从架构上看是可行的（只改 RecallEngine 的排序逻辑，不改存储）。`,
    labLinks: [{ tab: "context", label: "上下文分区与时间排序分析" }],
  },
  {
    id: "q1.17",
    question: '上下文水合：从冷层/温层取出的压缩记忆注入上下文前要不要"解压"？',
    chapter: "ch1",
    chapterTitle: "第 1 章：上下文工程",
    priority: "P2",
    confidence: { l0: 0.85, l1: 0.80, l2: 0.75, l3: 0.72 },
    overallConfidence: 0.72,
    l0: 'GlassCortex 中不需要解压——SQLite 存储的原始文本和召回输出的文本字段是同一个字符串，从冷层到热层直通。但在有真实压缩层（LLM 摘要）的架构中，"水合"是将压缩记忆恢复为上下文可用形式的过程。',
    l1: `"水合"（Hydration）这个术语借用了生物化学——干燥的蛋白质分子吸收水分后恢复活性。在记忆系统中同理：**记忆被压缩后处于"脱水"状态，需要恢复形状才能重新注入上下文**。

但有一个反直觉的结论：**在 GlassCortex 中，不存在水合问题。** 因为它的"压缩"不是信息压缩，而是选择——通过 \`simulate_overflow()\` 决定哪些记忆保留、哪些丢弃，而非将长文本压缩为短文本。

### 什么才是真正的压缩？

真实的记忆压缩通常发生在以下场景：

- **LLM 摘要压缩**：将一段长对话交给 LLM 概括为 1-2 句话
- **关键信息提取**：只保留对话中出现的实体、关系和事实（三元组）
- **向量化压缩**：用 Embedding 把文本压缩为向量（不可逆）

如果记忆被这样压缩后存入持久化存储，下次召回时就需要**水合**——把它恢复为模型可以理解的自然语言形式。

### 水合的三个层面

1. **格式转换**：数据库行 → 可读文本（最简单）
2. **上下文锚定**：标记"这是回忆，不是当前对话"（防止模型混淆）
3. **冲突核查**：检查重新注入的记忆与当前上下文是否有矛盾

GlassCortex 做了第 1 层（SQLite 到字符串的转换），但第 2 和第 3 层没有实现。`,
    l2: `### GlassCortex 的实际存储-召回路径

\`\`\`
用户输入 → Embedding → FAISS 语义搜索 → SQLite 取回原始文本 → 组装到上下文
         ↑              ↑                  ↑
    向量压缩(不可逆)    索引查询          SQL 行→行文本(水合点)
\`\`\`

关键代码调用链：

\`\`\`python
# src/memory/recall.py:42-128 — RecallEngine.recall()
# 1. Embedding
vec = self.embed(query)
# 2. FAISS 搜索
candidates = self.index.search(vec, k=search_k)
# 3. SQLite 取回完整行
episodes = self.store.get_episodes_by_faiss_id(faiss_ids)
# 4. 综合排序后返回（数据已是行文本格式，可直接用）
scored.sort(key=lambda x: x[1], reverse=True)
\`\`\`

在步骤 3 中，\`store.get_episodes_by_faiss_id()\` 返回的是 SQLite 中存储的 \`line\` 字段——这个字段存储的就是原始对话文本。**这里的"水合"只是 SQLite 字段到 Python 字符串的转换，复杂度为零。**

步骤 4 排序后的结果直接传给 \`_build_system_prompt()\`，后者将这些字符串组装到系统提示中。没有任何中间压缩层需要解压。

### 如果真的要水合

在需要真实水合的场景中，召回后的数据可能长这样：

\`\`\`python
# 假想的压缩态记忆（当前 GlassCortex 不存在）
compressed_memory = {
    "compressed_at": "2026-06-29T14:00:00",
    "original_length": 2048,      # 原始 token 数
    "compressed_length": 128,     # 压缩后 token 数
    "summary": "用户讨论 Python dict 性能",
    "entities": {"Python", "dict", "O(1)"},
    "format": "llm_summary_v1",
}
\`\`\`

要将其水合为可用上下文，需要：

1. 解包结构化字段 → 自然语言（"用户之前问过 Python dict 的性能问题，总结为：..."）
2. 添加水合标记（"以下内容是回忆，不是当前对话"）
3. 检查与当前上下文是否冲突（当前是否在讨论别的话题）

这些步骤在 GlassCortex 中都没有实现，因为当前没有压缩态记忆需要水合。`,
    l3: `### 水合的前沿思考

**1. 水合 ≠ 解压**
核心概念澄清：水合（Hydration）在系统设计中指的是"**将浓缩/序列化的数据恢复为可用形式**"，不一定是解压。在记忆系统的语境中，它包含：

- 格式转换：SQL 行 → 自然语言
- 身份标记：标记哪些记忆是重新注入的（而不是原始对话）
- 冲突核查：重新注入的记忆与当前上下文是否有矛盾

GlassCortex 当前只做了格式转换（SQLite → 字符串），没有身份标记和冲突核查。

**2. OpenAI 的 "Few-shot" 水合模式**
前沿实践中的一种水合方案是在注入压缩记忆时同时对模型做"再训练"——不是真的训练，而是在系统提示中强调"以下是你之前知道的关于这个话题的内容"。这种"上下文锚定"可以缓解水合后记忆与当前行为的不一致。

\`\`\`
# 系统提示中的水合锚定示例
"以下是从你的长期记忆中提取的相关信息：
<recalled>用户偏好：喜欢简短回答，专业术语偏好英文</recalled>
请自然地将这些信息融入对话中，但不要生硬复述。"
\`\`\`

**3. "即用即取" vs "预水合"**
- **即用即取**（Lazy Hydration）：只有确定某条记忆要注入上下文时才做格式转换——节省算力，但增加延迟
- **预水合**（Eager Hydration）：所有召回的记忆一次性转换为注入格式——延迟低，但可能浪费（实际没用上）
- **GlassCortex 的做法**：本质上是最简的预水合——SQLite 存储时已经保留了可用的文本字段，不需要额外转换

**4. 真正的压缩-水合循环**
在需要跨会话持久化压缩记忆的场景中，完整的压缩-水合循环应该是：

\`\`\`
[原始对话] → 压缩（LLM 摘要 / 关键信息提取）→ [压缩态存储]
    ↓（下次会话）
[压缩态] → 水合（格式恢复 + 上下文锚定 + 冲突核查）→ [注入上下文]
\`\`\`

这需要三个组件的配合：压缩器（Compressor）、存储层（Storage）、水合器（Hydrator）。GlassCortex 的 \`simulate_overflow()\` 的 summarize 策略可以看作一个雏形——但它只作用于内存（不持久化），也缺乏水合器。把它改造成持久化方案是一个自然的技术演进方向。

> 置信度：0.72

---

**相关 Lab**：在 Lab 的"数据"Tab 中，记忆浏览面板展示了 SQLite 中存储的原始对话记录——你可以观察记忆从持久化存储到上下文注入前的原始形态。`,
    labLinks: [{ tab: "data", label: "记忆浏览与存储格式" }],
  },
];